# Gate wall-clock baselines

Reference run times for `scripts/validate.sh`. Their purpose is not performance
tracking — it is **hang detection**. A gate that normally finishes in 17 s and is
still running at 300 s is deadlocked, not slow, and should be bisected as a bug
rather than retried with a bigger timeout.

Host: i7-870 (Nehalem, 4c/8t), Windows 11, MSYS2 CLANG64 build, `build/` (SoftRDP,
PRDP off). Numbers are wall clock, krom with the default 4 parallel jobs.

Measured 2026-08-18, commit = DPC counters + RSP threaded launch/PC-publication fix.

| Gate | interp | JIT | threaded |
|------|--------|-----|----------|
| systemtest (`n64-systemtest.z64 --run`) | 16-17 s | 25 s | 17 s |
| krom 371-ROM suite (4 jobs) | 238-250 s | 91-92 s | 140 s |
| SM64 300M ops (framebuffer md5) | 20 s | 15 s | 16 s |
| **`validate.sh all` total** | **~285 s** | **~132 s** | **~175 s** |

krom interp went 139 s -> ~240 s on 2026-08-18 when the gate stopped capping ROMs at a
fixed instruction count and started running them until the picture is finished
(`--stable/--maxflips/--maxsyncs`); the CPU decoders now run to completion. That is the
expected cost, not a regression. JIT/threaded rows are still the pre-change measurement.

Not part of `validate.sh` (run it by hand when the RDP cost model or the depth path
changes — see `docs/RDP-TIMING.md`):

| Battery | interp |
|---------|--------|
| RDP-Timing-Tests sweep (`rdp_fill_timing.z64`, `TOTAL_RUNS=8`, 100 configs) | 97 s |

systemtest's own internal report ("Finished in N s") is ~7 s in every mode; the rest
of the wall time is ROM load plus boot. The historical 6.86 s quoted throughout
STATUS.md is that internal figure, not wall clock — do not compare the two.

`validate.py` default timeouts: `--st-timeout 300`, `--sm64-timeout 600`,
`--timeout 90` (per krom ROM). Those are ~17x the measured systemtest time, so a
timeout there always means a genuine hang.

## Known hang signature (fixed 2026-08-18)

`systemtest --mode threaded` froze at *"Running RSP VRSQ (all 16 bit values)"* after
~20 s of progress and never advanced. Two distinct threaded-only bugs, both real
RCP-semantics violations, not test-specific quirks:

1. **Dropped RSP launch.** A lone CLEAR_HALT write was gated on the emulator's
   `rspBusy` worker flag, so a launch issued while the worker was still winding down
   from the previous task was silently discarded — the task never ran and the CPU
   polled a BREAK that never came. Launches are now gated on the RSP's actual HALT
   bit (the hardware condition) and ordered behind the wind-down (`rspAwaitIdle`).
2. **PC writeback published after HALT.** `Rsp::step()` set `HALT|BROKE` at BREAK and
   only afterwards wrote `sp_pc` back. The CPU treats HALT as "task over" and
   immediately writes the next task's SP_PC, which the late writeback then clobbered
   — the next task started at the old BREAK and produced nothing (`a=0x0` in
   `RSP VRCP (all 16 bit values)`, ~1 failure per 65536 iterations, nondeterministic).
   BREAK now only latches `broke`; the status publish happens after the PC writeback.

`Rcp::sp_status` was also made `std::atomic<u32>` — one register updated by both the
CPU (control writes) and the RSP worker (BREAK) cannot be a plain `u32` without
losing updates.

## Entorno del gate (dos fallos que parecen bugs y no lo son)

`scripts/validate.py` necesita a la vez:

- el **python de Windows** (`~/AppData/Local/Programs/Python/Python311/python`), que es el
  que tiene numpy — el de MSYS/CLANG64 no lo tiene, y sin numpy las 371 ROMs salen `CMPERR`;
- `/c/msys64/clang64/bin` en el **PATH**, porque el exe carga sus DLL de ahi — sin eso
  arranca con `0xC0000135` y las 371 salen `NODUMP rc=3221225781`.

Es decir: `export PATH=/c/msys64/clang64/bin:$PATH` y llamar al python de Windows por ruta.
`preflight()` comprueba las dos cosas y aborta en un segundo en vez de a los cuatro minutos.

## 2026-08-19 — `gate_all.sh` (cinco modalidades + krom) y la puerta `bench`

`sh scripts/gate_all.sh` = systemtest + sm64 en interp / jit / jit-nolink / threaded /
threaded-jit, luego krom. Total medido: **~7 min**.

| Tramo | interp | jit | jit-nolink | threaded | threaded-jit |
|---|---|---|---|---|---|
| systemtest | 15 s | 9 s | 9 s | 16-17 s | 9 s |
| sm64 (60 campos) | 13-14 s | 8 s | 9-10 s | 11 s | 3-4 s |
| krom 371 ROMs (4 jobs) | 163-167 s | — | — | — | — |

`validate.py bench --bench-runs 2` (600 campos VI de SM64, dos pasadas) = ~25 s en
threaded-jit, ~160 s en interp. Si una pasada de threaded-jit pasa de ~15 s con la maquina
ociosa, hay contencion (otro gate corriendo) o una regresion; no subir el timeout.

## 2026-08-20 — `gate_all.sh` medido, y la prueba larga de SM64

`sh scripts/gate_all.sh`: **282-293 s** de reloj de pared (tres medidas: 282, 283, 293 s),
maquina ociosa. El propio script imprime `GATE_WALL=<s>` al final; si sale muy por encima
de ~300 s, es contencion o cuelgue, **no** subir el timeout.

Prueba larga de cuelgue (la que destapo la carrera del FIFO del RDP):

```
KESTREL_THREADS=1 KESTREL_JIT=1 KESTREL_HANGDOG=8 KESTREL_NOVIDEO=1 \
KESTREL_MAXINSN=99000000000 KESTREL_MAXFLIPS=2500 ./build/kestrel64.exe "<sm64>" --run
```

= **118-120 s** para 2500 campos (~8.6 G instrucciones), y debe terminar con
`[frames] 2500`, sin lineas `[rdp!]` ni `[hangdog]`. Borrar el `.eep` antes: con partida
guardada la demo de attract toma otro camino y la comparacion no vale. Antes del fix esta
misma prueba se iba a los 300 s y moria por timeout, con ~53 anomalias `[rdp!]`.

## 2026-08-20 — likely-branches en el JIT + regulador CPU<->RSP (Threaded)
- `sh scripts/gate_all.sh`: **ALL OK**, krom regress=0, md5 SM64 `466282775dbd0ac084946558a1c30771`
  en los 5 modos. Wall ~285s (baseline 282-293s).
- SM64 2500 swaps (`KFLIPS=2500 sh scripts/perf.sh`): **110.5s** (antes 119s), `[frames] 2500`,
  sin `[rdp!]` ni `[hangdog]`.
- SM64 500 swaps: 20.9s -> **20.2s**, 23.92 -> **24.70 swaps/s**, 1607M -> **1223M** instrucciones
  de guest (el regulador quita el giro en vacio, no trabajo util).

## 2026-08-20 — permiso de guarda con regulacion (threaded)

- `gate_all.sh`: ALL OK, krom regress=0, md5 sm64 `466282775dbd0ac084946558a1c30771`.
- SM64 2500 swaps (threaded-jit): **110.8s / 22.55 swaps/s / 6678M insns / anom=0**
  (antes del regulador: 119s).
- SM64 300 swaps: 24.50 → 25.21 swaps/s (+2.9%) al mover la regulacion CPU<->RSP dentro
  del permiso (`jitGuard`) y quitar la guarda `rsp.running` del codigo emitido en Threaded.
- Barrido `KESTREL_PACESLACK` {64,256,1024,8192,32768,131072}: el default 8192 es el mejor.
- `--vubench`: 21.28 → 18.27 ns/op tras quitar la division entera del propio banco de pruebas
  y alinear `R128` a 16 (RSP: 34.9 → 36.1 Mips).

## 2026-08-20 — interprete del RSP: donde se va el tiempo (banco `--rspbench`)

`--vubench` solo medía la ALU vectorial, que resultó ser ~52% del tiempo del RSP. Para ver el
resto se añadió **`--rspbench`**: llena la IMEM entera (1024 ranuras, el PC envuelve solo) con
la mezcla de opcodes medida sobre el arranque de SM64 — 386 COP2, 123 SPECIAL, 103 LWC2, 83
SWC2, 213 ADDI, 38 LH, 37 ANDI, 24 LW, 17 SW de cada 1000 — y con las subfamilias pesadas
igual (VMADN 17%, VMADH 13%, ... / LDV 9 de cada 25 LWC2). `KESTREL_RSPMIX=<familia>` aísla
una: `cop2`, `vecld`, `alu` (ADDI rotando destino), `alu1` (ADDI encadenado sobre el mismo
registro), `scald` (LW/SW), `nop` (SLL r0,r0,0 = coste del bucle desnudo).

Anfitrión i7-870, 2.93 GHz **medidos** (`Win32_Processor`), 1 ciclo = 0.341 ns.
**El N64 pide 62.5 Mips.**

| mezcla | antes | después |
|--------|-------|---------|
| real   | 18.8 ns / 53.2 Mips | **15.45 ns / 64.7 Mips** |
| nop    | 8.35 ns | 7.16 ns |
| alu    | 8.10 ns | 6.04 ns |
| scald  | 10.1 ns | 6.89 ns |
| cop2   | 25.9 ns | 23.6 ns |
| vecld  | 25.1 ns | 22.5 ns |

### Lo que lo explica: este anfitrión despacha UNA carga por ciclo

Nehalem tiene un solo puerto de carga. En un intérprete eso convierte cada campo del objeto
leído dentro del cuerpo del bucle en un ciclo entero, y el bucle leía doce: `budget`, `halt`,
`imp`, `pc`, la palabra de IMEM, `profOn`, la tabla de saltos, `r[rt]`, `inDelay`, `branch`,
`pendingTarget`... El suelo medido con `nop` era 8.35 ns = **24 ciclos para no hacer nada**.

Dos cambios (commit `fa308ef`), ambos de semántica idéntica:

1. **`dmp`/`imp` como punteros del propio `Rsp`** (`bindMem()`) en vez de `mem->dmem.data()`
   en cada acceso: `std::vector::data()` se recargaba en cada uso porque cualquier llamada
   interna podía aliasar el vector. **53.2 → 56.9 Mips (+7%)**.
2. **Bucle por tandas de 8K instrucciones**: fuera del cuerpo el puntero a IMEM, el
   interruptor del muestreador y los tres contadores (`maxInsns`, `budget`, `ran`) reducidos
   a uno solo. **56.9 → 64.7 Mips (+13%)**. La tanda es la misma que ya usaba la publicación
   al regulador, así que el hilo CPU no ve el avance más tarde que antes.

### Lo que se probó y NO valió (medido, revertido)

- `execVuSse(u32 fn, __m128i t, ...)` pasando el vector por valor: **peor** (17.3 → 20.0 ns).
  Win64 pasa `__m128i` por valor **en memoria**, así que la tienda que se quería quitar vuelve.
- La misma función tomando `(vt, e)` y haciendo el broadcast dentro: peor (18.6-18.8 ns).
  Dentro del callee `vpr[vt]` puede aliasar la salida `R128& D` y la carga deja de poder
  adelantarse; el temporal del llamante demostrablemente no aliasaba.
- Reescribir `vadd48` con cadena de acarreo (`vcarry16` vía xor 0x8000 + cmpgt): correcto
  (`--rspfuzz` 0 fallos) pero ~2% más lento: cambia rendimiento por latencia en serie.
- `flatten` en `Rsp::exec` además de en `step`: 44.8 vs 48.2 Mips, **peor**. En `step` solo: +3.2%.
- Mantener `pc`/`budget`/el latch de retardo en locales: **peor** (real 17.8 → 18.3 ns). El
  registro extra hace derramar otras cosas dentro del switch gigante.
- Leer la instrucción siguiente por adelantado (con un contador de generación de IMEM para el
  microcódigo que se reescribe): neutral en la mezcla real. No compensa ni la carga extra ni
  el riesgo.

### Y sin embargo, en SM64 no se nota — y eso es el dato importante

`KFLIPS=250 sh scripts/perf.sh`, media de 5 pasadas: **24.78 → 24.79 swaps/s**, y el latido
sigue diciendo **`rsp 35.3 Mips busy`** igual que antes del +21% aislado. O sea: el tiempo
del worker del RSP **no** es ejecutar instrucciones. Lo que hay dentro de `step()` y no
ejecuta instrucciones es el DMA del SP (una instrucción `MTC0` que mueve kilobytes).

Barrido de elasticidad sobre SM64 (250 swaps, attract intercambia a 30/s = tiempo real):

| variante | swaps/s | % de tiempo real |
|----------|---------|------------------|
| base                        | 25.20 | 84% |
| `KESTREL_NORASTER=1`        | 28.95 | **96%** |
| `KESTREL_PACESLACK=1000000` | 23.82 | 79% |
| `KESTREL_JIT=0`             | 10.42 | 35% |
| `KESTREL_THREADS=0`         |  6.65 | 22% |

**Conclusión: SM64 corre al 84% de tiempo real y casi todo el hueco que queda es el
rasterizador por software.** El siguiente palo largo no es el intérprete ni el JIT: es
parallel-RDP.

---

## 2026-08-20 — parallel-RDP: de regresión a empate (RX 570)

Punto de partida medido (SM64, `KFLIPS=250`, `build-prdp`):

| variante | swaps/s |
|----------|---------|
| SoftRDP (`KESTREL_PRDP` sin poner) | 23.95 |
| parallel-RDP (`KESTREL_PRDP=1`)    | **19.54** |

El backend de GPU era **más lento** que el rasterizador por software. Dos causas reales,
las dos arregladas.

### 1. La RDRAM no estaba alineada a página → cero copia perdida

parallel-rdp intenta importar la RDRAM del invitado directamente en la GPU con
`VK_EXT_external_memory_host` (`create_imported_host_buffer`): la GPU lee y escribe la
memoria del emulador *sin copia*. La importación exige que el puntero esté alineado a
`minImportedHostPointerAlignment` = **4096 B** en escritorio. `Memory::rdram` era un
`std::vector<u8>` corriente, que solo garantiza 16 B:

```
[vrdp] gpu="Radeon RX 570 Series" ext_mem_host=1 align=4096 rdram=0000018F2767B040  MISALIGNED -> slow copy path
```

Al fallar, parallel-rdp cae **en silencio** (el LOGW va al hilo con la interfaz de log
silenciada) a un espejo de 16 MB en la GPU con máscara de escritura, que hay que sincronizar
alrededor de cada sync: 8 MB de PCIe por campo.

Arreglo: `AlignedAllocator<u8, 4096>` en `core/types.hpp` y `using GuestBytes` para la
RDRAM. Los ayudantes `rd32/rd64/wr8/wr16/wr32` de `rdp/rdp.cpp` pasan a plantilla sobre el
contenedor. Nada de esto toca semántica de la máquina: es alineación del host.

**19.54 → 21.69 swaps/s (+11%).**

### 2. El scanout esperaba a la GPU dos veces por campo

`produceScanout()` se hacía **dentro del SYNC_FULL** y esperaba el fence al momento:
`scanout_async_buffer()` → `fence->wait()` → `map` → `memcpy`. Es decir, justo después de
haber esperado ya la línea de tiempo del DP, se volvía a bloquear el hilo esperando a la GPU.
Cero solape.

Modelo correcto (y el que usa ares): **el scanout lo manda el VI, no el DP**. El VI escupe
lo que haya en el framebuffer en cada frontera de campo, pase lo que pase con el pipe del
RDP. Ahora `pumpViState()`, cuando ve la frontera de campo:

1. `harvestScanout()` — recoge el campo *anterior* (su fence ya está firmado hace rato),
2. `begin_frame_context()`,
3. `issueScanout()` — pide el siguiente y **no** espera.

La GPU trabaja un campo entero mientras el emulador sigue.

| tiempo del hilo RDP (250 campos, `KESTREL_PRDP_STATS=1`) | antes | después |
|---|---|---|
| recorrer/encolar la FIFO | 397 ms | 408 ms |
| esperar la línea de tiempo en SYNC_FULL | 615 ms | 549 ms |
| scanout (fence + map + memcpy) | **579 ms** | **0.55 ms** |

**21.69 → 23.17 swaps/s.** El scanout deja de existir como coste: el fence siempre llega ya
firmado.

### Resultado y techo

| variante | swaps/s | % de tiempo real (30/s) |
|----------|---------|------------------------|
| parallel-RDP inicial | 19.54 | 65% |
| + RDRAM alineada     | 21.69 | 72% |
| + scanout encauzado  | **23.17** | 77% |
| SoftRDP              | 24.61 | 82% |
| sin rasterizar (`KESTREL_NORASTER=1`) | 28.95 | 96% |

En esta máquina parallel-RDP **empata** con SoftRDP, no gana. Lo que queda es
`wait_for_timeline` en cada SYNC_FULL: 549 ms / 250 = **2.2 ms por campo** de latencia
GPU+driver, y esa espera está en el camino serie del invitado (el juego duerme hasta la
interrupción del DP). Es latencia de sumisión de una RX 570 en Windows, no trabajo nuestro.

Ruido pendiente, medido pero no arreglado: **641 fallos de reserva por campo-y-pico**
(`Exhausted LinkedDeviceHost memory` + `Will exceed memory budget` + `garbage_collect`).
Granite quiere un bloque de 64 MiB en el heap BAR host-visible (256 MiB en la RX 570, ~199
MiB ya ocupados por el escritorio) para un búfer de pocos cientos de bytes que
`bind_horizontal_info_view()` crea en cada pasada del VI; falla, recolecta basura y cae a
memoria de sistema **cada vez**, sin recordar el fallo. Arreglarlo exige parchear el árbol
de parallel-rdp, que hoy vive dentro del checkout **congelado** de ares
(`PRDP_DIR`); el paso limpio sería vendorizar parallel-rdp dentro de kestrel.

## `gate_all.sh` completo (2026-08-18/20)

Medido tras el fix de contrapresion del FIFO del RDP (`docs/RDP-FIFO-BACKPRESSURE.md`),
mismo host. Dos pasadas consecutivas dieron lo mismo dentro del ruido: **4 min 53 s**
de reloj de pared en total (15:56:03 -> 16:00:56).

| Tramo | interp | jit | jit-nolink | threaded | threaded-jit |
|-------|--------|-----|------------|----------|--------------|
| systemtest | 16-17 s | 9-11 s | 9 s | 17-18 s | 9-10 s |
| sm64 (60 campos VI) | 14 s | 7 s | 9-10 s | 11-12 s | 2-3 s |
| krom 371 ROMs (4 jobs, solo interp) | 164-186 s | — | — | — | — |

Los tramos JIT bajaron mucho respecto a la tabla de arriba porque aquella medicion
era anterior al block linking.

Re-medido 2026-08-20 con el reloj del VI unificado (`docs/VI-CLOCK.md`) y el gate de krom
parando en 2 SYNC_FULL en vez de 1: systemtest 16 s / 8-9 s / 9 s / 18 s / 9-10 s, sm64
14-15 s / 7 s / 10 s / 11-12 s / 2 s, krom 181-246 s. Igual dentro del ruido; los 246 s de
krom son el coste de dejar terminar la segunda display list.

El gate de sm64 para a los 60 **campos de video**, que es un estado del juego y no del
reloj de pared: por eso threaded-jit tarda 2 s y interp 14 s para la misma imagen. Esa
diferencia entre modos es normal; lo que seria fallo es que el md5 no coincidiera.

Si un tramo pasa de ~3x su fila, es cuelgue: bisecar con `git stash`, no subir el timeout.

### Pasada con el dynarec del RSP (2026-08-23)

`gate_all.sh` completo **5 min 53 s** (18:54:51 -> 19:00:44) con `KESTREL_RSPJIT` por
defecto ON. systemtest 24/16/17/18/10 s, sm64 14/6/9/11/2 s, krom 218 s (4 jobs).
La diferencia con los 4 min 53 s de la fila anterior esta toda en krom, que ya venia
oscilando 181-246 s entre pasadas; ningun tramo se salio de su rango.
- gate_all (rebuild build/, 5 modos + krom 371): **5m30s** (2026-08-23, tras fix rdpDrain+FBDUMP). Desglose: systemtest 26/16/18/19/11s, sm64 15/6/9/11/2s, krom 214s @4 jobs.
- krom 371 ROMs bajo `KESTREL_PRDP=1` (build-prdp, RX 570): **266s** @4 jobs.
- gate_prdp (systemtest lockstep + sm64 x2 + krom 371 con KESTREL_PRDP=1): **5m20s** (2026-08-23, RX 570). krom PRDP 259s @4 jobs.

| 2026-08-26 | gate_prdp.sh | 299 s | gate_all.sh | 343 s | fence SYNC_FULL medido (sin cambio funcional) |
| 2026-08-26 | gate_all.sh | 432 s | gate_prdp.sh | 326 s | COP2 especializado por fn |
| 2026-08-26 | gate_all.sh | 357 s | gate_prdp.sh | 312 s | sync RCP: coalesce FIFO RDP + notify condicional + dmaCopy + paceSlack 512K |
| 2026-08-26 | gate_all.sh | 350 s | gate_prdp.sh | 301 s | sin drenado RDP en START + getenv cacheado + stack-scan filtrado |
| 2026-08-26 | gate_all.sh | ~370 s | gate_prdp.sh | ~320 s | getenv cacheado en cpu.cpp (MTC0 Status / ERET / excepciones) |
| 2026-08-26 | gate_all.sh | 361 s | gate_prdp.sh | 303 s | JIT: spill perezoso + forget dirigido + COP1 directo + helpers de memoria especializados |
| 2026-08-26 | gate_all.sh | 359 s | gate_prdp.sh | 304 s | JIT: LWC1/LDC1/SWC1/SDC1 por helper especializado |
| 2026-08-26 | gate_all.sh | 363 s | gate_prdp.sh | 309 s | JIT: MFC1/DMFC1/CFC1/MTC1/DMTC1 por trampolin propio |
| 2026-08-26 | gate_all | 426 s | fpalu + kPaceSlack 256K; regress=0, 12/12 |
| 2026-08-26 | gate_prdp | 311 s | idem; regress=0 |
| 2026-09-02 | gate_all | 356 s | menu en ventana (optdefs/profile/runtime/menu_win32); 15 ALL OK, krom regress=0, md5 sm64 466282775dbd0ac084946558a1c30771 |
| 2026-09-02 | gate_prdp | 296 s | idem; 4 ALL OK, krom PRDP regress=0 |
| 2026-09-02 | gate_all | 354 s | QA del menu (colision de IDs volumen/velocidad/ranura, barra fuera en pantalla completa, validacion numerica, cierre de dialogos, puerto de telemetria libre al relanzar); 15 ALL OK, regress=0 |
| 2026-09-02 | gate_prdp | 296 s | idem; 4 ALL OK, regress=0 |
| 2026-08-27 | gate_all | 390 s | limitador de velocidad; 12/12, regress=0 |
| 2026-08-27 | gate_prdp | 322 s | idem; regress=0 |
| 2026-08-27 | gate_all | ~400 s | contexto Vulkan compartido + candado de cola; 12/12, regress=0 |
| 2026-08-27 | gate_prdp | ~330 s | idem; regress=0 |
| 2026-08-27 | gate_all | ~350 s | RDP no ejecuta comandos partidos; 12/12, regress=0 |
| 2026-08-27 | gate_prdp | ~305 s | idem; regress=0 |
| 2026-08-27 | gate_all | ~350 s | conversiones COP1 en el JIT; 12/12, regress=0 |
| 2026-08-27 | gate_prdp | ~305 s | idem; regress=0, improve=1 |
| 2026-08-27 | gate_all | ~351 s | C.cond de COP1 en el JIT; 12/12, regress=0 |
| 2026-08-27 | gate_prdp | ~319 s | idem; regress=0 |
| 2026-08-27 | gate_all | ~351 s | DIV y CVT.S.W/CVT.D.W en el JIT; 12/12, regress=0 |
| 2026-08-27 | gate_prdp | ~319 s | idem; regress=0 |
| 2026-08-27 | gate_all | ~351 s | xlatDirect en linea + cacheable en linea; 12/12, regress=0 |
| 2026-08-27 | gate_prdp | ~320 s | idem; regress=0 |
| 2026-08-27 | gate_all | ~397 s | D-cache en linea; 12/12. krom: 4 NODUMP transitorios, limpio al re-correr (regress=0) |
| 2026-08-27 | gate_prdp | ~487 s | idem; regress=0. Las dos cifras suben por el krom re-corrido en paralelo |
| 2026-08-27 | gate_all | ~339 s | validacion SMC por linea de I-cache; 12/12, regress=0 |
| 2026-08-27 | gate_prdp | ~307 s | idem; regress=0 |
| 2026-08-27 | gate_all | 412 s | gate_prdp | 322 s | JIT: CACHE en bloque + ranura de retardo al interprete; 12/12, regress=0 (tiempo NEUTRO, ver PERF-RCP-SYNC 9) |
| 2026-08-27 | gate_all | 420 s | gate_prdp | 331 s | overclock (KESTREL_OC*) + tamano de ventana/pantalla completa; 12/12, regress=0 |
| 2026-08-27 | gate_all | 422 s | gate_prdp | 315 s | mando configurable (KESTREL_PAD1); 12/12, regress=0 |
| 2026-08-27 | gate_all | 430 s | gate_prdp | 332 s | JIT: guardia R==0, privilegio ckseg0, arnes jitdiff, ALU-con-trampa por defecto=3; 12/12, regress=0 |
| 2026-08-27 | gate_all | 398 s | gate_prdp | 307 s | JIT: camino rapido de memoria en el bloque (LW/SW/LWC1/SWC1); 12/12, regress=0 en ambos |
| 2026-08-27 | gate_all | 357 s | gate_prdp | 296 s | JIT: camino rapido de memoria extendido a los 15 opcodes (1/2/4/8 B, enteros y COP1); 12/12, regress=0 en ambos |
| 2026-08-27 | gate_all | 381 s | gate_prdp | 298 s | JIT: BC1F/BC1T/BC1FL/BC1TL absorbidos como salto nativo; 12/12, regress=0 en ambos |
| 2026-08-27 | gate_all | 360 s | gate_prdp | 305 s | Producto: --play, arranque desde el Explorador, KESTREL_STATIC; 12/12, regress=0 en ambos |
| 2026-08-27 | gate_all | 363 s | gate_prdp | 302 s | JIT: `memAbort` limpiado al entrar al bloque (ADDI absorbida por defecto) + reloj de decaimiento del latch PI con `jitPending`; 12/12, regress=0 en ambos |
| 2026-08-27 | gate_all | 362 s | gate_prdp | 303 s | RSP JIT: invalidacion de IMEM por contenido + cobertura exacta del bloque (compilaciones 1.70M -> 1.37M en SM64/600 campos); 12/12, regress=0 en ambos |
| 2026-08-28 | gate_all | 354 s | gate_prdp | 303 s | JIT: condicion de salto en R15 + condicion constante plegada; tag de D-cache con el bit de valida dentro (linea 32 B, una sola comparacion) + guardia unica de store (CPU::stGuard); volcado KESTREL_JIT_DUMP y muestreo de RIP crudo del JIT en hostprof; 12/12, regress=0 en ambos |
| 2026-08-28 | gate_all | 355 s | gate_prdp | 303 s | RSP JIT: cache de imagenes de microcodigo por parecido (N tablas, KESTREL_RSPJIT_WAYS=4) + syncImem sin clear() masivo; 12/12, regress=0 en ambos |
| 2026-08-28 | gate_all | 352 s | gate_prdp | 304 s | RSP: VSAR + familia del reciproco fuera del switch gordo (execCop2Div); bench prdp 10.06 -> 9.68 s; 12/12, regress=0 |
| 2026-08-28 | gate_all | 377 s | gate_prdp | 322 s | RSP JIT: VU en linea (logicas+VSAR+VADD/VSUB/VADDC/VSUBC/VMUDL) + `--rspjitfuzz` (200k bloques, 0 dif); lockstep 300 flips 31.95 -> 31.80 s; 12/12, regress=0. Tiempos altos = compilaciones en paralelo al arranque de la puerta |
| 2026-08-28 | gate_all | 363 s | gate_prdp | 302 s | RSP: `vcarry16` (acarreo por banda de 16 b) en `vadd48`/VMADH + RSP JIT: VMUDM/VMUDN/VMUDH/VMADH en linea; presentador: recreacion de la cadena de intercambio al redimensionar + blit con relacion de aspecto; 12/12, regress=0 en ambos |
| 2026-08-28 | gate_all | 366 s | gate_prdp | 316 s | RSP JIT: familia MAC entera en linea (VMULF/VMULU, VMACF/VMACU, VMADL/VMADM/VMADN) + `--rspjitfuzz` con rotacion de opcodes ampliada (400k bloques, 0 dif); 12/12, regress=0 en ambos |
| 2026-08-28 | gate_all | 358 s | gate_prdp | 321 s | RSP JIT: VLT/VEQ/VNE/VGE + VMRG + VABS en linea (`emitSelectST`, `emitFlagMask/Store`); fuzz 400k bloques con la rotacion ampliada, 0 dif; 12/12, regress=0 en ambos |
| 2026-08-28 | gate_all | 354 s | gate_prdp | 303 s | RSP JIT: acumulador en xmm6..xmm8 (cache accValid/accDirty). Verde tras arreglar el volcado de emitMem (CALL bajo condicion). 12/12 + prdp 3/3, regress=0 en ambos krom |
| 2026-08-28 | gate_all | 357 s | gate_prdp | 313 s | RSP JIT: VMOV/VRCPH/VRSQH en linea (pextrw + store de 16 bits) + fuzz con la familia del reciproco y el estado divin/divout/divdp; 400k bloques 0 dif; 12/12, regress=0 en ambos |
| 2026-08-28 | gate_all | 367 s | gate_prdp | — | RSP JIT: VCH/VCL/VCR en linea (derrame xmm9..xmm13 solo si el bloque las lleva; REX en las formas de desplazamiento por inmediato y en las 0F 38, que faltaba); fuzz 500k bloques 0 dif; 6/6, regress=0 |
| 2026-08-28 | gate_all | 366 s | gate_prdp | 309 s | RSP JIT: familia del reciproco en linea (VRCP/VRCPL/VRSQ/VRSQL, enteros + tabla de 512, `bsr` porque Nehalem no tiene LZCNT); fuzz 500k bloques 0 dif; 12/12, regress=0 en ambos |
| 2026-08-28 | gate_all | 387 s | gate_prdp | 342 s | RSP JIT: LWC2/SWC2 pares en linea (`emitVecMem`, vuelta al helper por `jcc8` cuando la direccion envuelve o no esta alineada); fuzz 500k bloques con DMEM+r[] completos, 0 dif; 12/12 + prdp 3/3, regress=0 en ambos; lockstep jit 8.83->8.76 s |
| 2026-08-28 | gate_all | 396 s | gate_prdp | 334 s | RSP JIT: movimientos escalar<->vector en linea (MFC2/CFC2/MTC2/CTC2, sin envoltura de respaldo; `bitLane[8]` constante dentro de `Rsp` para CTC2); fuzz ahora genera sub<0x10, 500k bloques 0 dif, cobertura probada rompiendo los 4 cuerpos; 12/12 + prdp 3/3, regress=0; lockstep jit 8.64->8.55 s |
| 2026-08-28 | gate_all | 395 s | gate_prdp | 342 s | Controller Pak por joybus (`0x00/0x02/0x03` en el canal 0, CRC5 de direccion + CRC8 de datos del SDK, pak formateado de fabrica, `.mpk` junto al ROM, entra en el savestate `kVersion` 2); `save_test` con seccion nueva ALL PASS (CRC recalculados aparte, no reusados); 12/12 + prdp 3/3, regress=0 en ambos, sm64 md5 sin cambio |
| 2026-08-28 | gate_all | 338 s | gate_prdp | 316 s | RSP JIT etapa 3: enlace de bloques por cola (`jmp` tras desmontar el marco, sondeo indirecto por tabla, destino estatico en caida y J/JAL, saldo debitado por quien salta) — despachos 7,37 M -> 1,25 M (-83 %), rspbench +2,4 %; ademas guarda `c >= kMinOps` en el despachador: en Lockstep se compilaban bloques que nunca cabian en `step(1)`, `bench --mode jit` 24,10 -> 22,60 s (-6,2 %); gate_all ahora 14 pasos (modo `rspnolink`), 14/14 + prdp 3/3, regress=0, md5 sin cambio |
| 2026-09-01 | gate_all | 339 s | gate_prdp | 290 s | Presentador: imagen fuente del tamano real del cuadro (antes fija 320x240 -> con parallel-rdp se veia media imagen), altura por Y_SCALE, aspecto 4:3 fijo, HUD escalado. 14/14 + prdp 3/3, regress=0, md5 sin cambio |
| 2026-09-01 | gate_all | 334 s | gate_prdp | 297 s | Cache de imagenes del RSP JIT recalibrada (ranuras 4->16, umbral 64->8): bloques compilados 241162 -> 3948 en SM64/500, rsp 144.9 -> 186.9 Mips ocupado, pace 18% -> 9%. 14/14 + prdp 3/3, regress=0, md5 sin cambio |
| 2026-09-01 | gate_all | 339 s | gate_prdp | 311 s | MUL.S sin MXCSR (producto exacto en `double`, Inexact por comparacion, solo el modo de redondeo se escribe). 14/14 + prdp 3/3, regress=0, md5 sin cambio |
| 2026-09-01 | gate_all | 335 s | gate_prdp | 310 s | LPV/LUV/LRV en linea en el RSP JIT (`pshufb` con mascara armada al vuelo, tablas `byteHalf`/`laneIdx`). rspjitfuzz+rspldfuzz 300k cada uno = 0 diferencias. 14/14 + prdp 3/3, regress=0, md5 sin cambio |
| 2026-09-01 | gate_all | 334 s | gate_prdp | 296 s | Regulador de ritmo recalibrado (`kPaceSlack` 256 K -> 1 M): +16 % de fotogramas de juego/s con parallel-rdp (300 intercambios 4,40 -> 3,80 s), SoftRDP plano; campos VI/intercambio 2,73 -> 3,24 acercandose al oraculo lockstep (4,09). El "% realtime" NO vale para calibrar esto (infla campos sin trabajo). 14/14 + prdp 3/3, regress=0, md5 sin cambio |
| 2026-09-01 | gate_all | 339 s | gate_prdp | 289 s | Cobertura del JIT de CPU: ranura de retardo con ALU-que-atrapa y MFC0, bloques que cruzan pagina 4K solo desde ckseg0 (marca `crossPage`, el despacho la rechaza por ruta TLB; la cache negativa lleva la ruta), MTC0/ERET/TLBR/TLBWI/TLBP como op TERMINAL (fuera Count/Compare/Random/Wired/TLBWR, los caza n64-systemtest), MFC0 Count dentro del bloque (`cop0[Count] + idx`, exacto) y ALU de 64 bits bajo guardia de kernel (`emitAlu64`). Lideres no compilables 614 k -> 25 k por ventana, ops/entrada 333 -> 445. 14/14 + prdp 3/3, regress=0, md5 sin cambio |
| 2026-09-01 | gate_all | 345 s | gate_prdp | 306 s | SoftRDP tanda 1 (el hilo del RDP manda: ocupacion 89 % vs RSP 58 %, cpuWait 0 %): `getenv` por triangulo y `std::lround` fuera del camino caliente (17,3 % del hilo), pliegue del tile una vez por pixel (`TexFold`), formato especializado (`texelK<K>`/`filterK<K>` con `always_inline` -- sin el atributo clang NO alineaba RGBA16, 35 % del perfil), invariantes de primitiva fuera del bucle, y plan del combinador (llave = combine_hi/lo + cycleType, NOISE cae al camino generico para no tocar el orden de `std::rand()`). Pared SM64/300 intercambios 7,89 -> 5,68 s. 14/14 + prdp 3/3, regress=0, md5 sin cambio |
| 2026-09-01 | gate_all | 334 s | gate_prdp | 305 s | SoftRDP tanda 2: plan del blender (llave other_hi/other_lo), `storePixel` sin el test de scissor repetido tres veces por pixel, `loadTile` con `memcpy` por fila (recorte una vez, mismo prefijo exacto), **combinador en SSE4.1** (4x int32, carril de alpha via `_mm_blend_epi16` 0xC0; escalar como referencia y camino NOISE) del 38 % al 19 % del hilo, division del blender a multiplicador magico (rangos acotados por construccion, exactitud comprobada exhaustivamente) y lectura de framebuffer elidida cuando el atajo del blender la iba a tirar. Pared SM64/300 intercambios 5,68 -> 4,93 s; acumulado del dia 7,89 -> 4,93 (-37,5 %). 14/14 + prdp 3/3, regress=0, md5 sin cambio |
| 2026-09-01 | gate_all | 338 s | gate_prdp | 294 s | SoftRDP tanda 3: sombra+S/T por escanlínea en SSE, filtro de 3 tomas en SSE, `texelK` partido en `foldCoord`+`fetchK` (4 pliegues/píxel en vez de 6). 14/14 + prdp 3/3, regress=0, md5 sin cambio. Pared SM64/300 flips 4,93 → 4,46 s. |
| 2026-09-02 | gate_all | 346 s | gate_prdp | 430 s | parallel-rdp por defecto (cmake + runtime, con caida a SoftRDP si Vulkan no sirve) + `Rsp::mtc0` por `rcpRegWrite32` + fusion de `rcpPace`/`paceAllowance` (una sola lectura de las lineas del worker) + ADD.S/SUB.S exactos en `double` bajo `|ea-eb| <= 28` (oraculo `jitCop1AluChk`, 6,3 M comparaciones sin diferencias). 14/14 + prdp 3/3, regress=0 en ambos krom, md5 sin cambio. krom prdp 398 s (4 trabajos) explica el gate_prdp largo. |
| 2026-09-02 | gate_all | 339 s | gate_prdp | 305 s | Audio: el DAC del AI drena por muestras (credito en instrucciones retiradas) en vez de ≤ un bufer por campo — SM64 encola bufers de ~0,75 campos, asi que se quedaba en FIFO_FULL y generaba el 75 % del audio; silencio 24,63 % → 0,38 %, recargas cortas 394 → 6. Sumidero waveOut con colchon de arranque, histeresis de tasa y anillo por memcpy; `KESTREL_AUDIOSTAT=1`. JIT de CPU: **ITC** (cache de destinos indirectos para JR/JALR, 4096 entradas de 16 B, sonda dentro del bloque, `KESTREL_JIT_NOITC=1` la apaga) — 200 intercambios 3,19 → 3,12 s (≈2,5 %). 14/14 + prdp 3/3, regress=0 en ambos krom (mean_exact interp 88,71 / prdp 89,26), **nodump=0** en los dos: los 3 NODUMP de la tanda anterior eran contencion de GPU por correr sm64 a la vez que la puerta, no un fallo. md5 sin cambio. |
| 2026-09-02 | gate_all | 355 s | gate_prdp | 298 s | ITC afinada: indice mezclado `((va ^ (va>>12))>>2) & mask` (605 -> 620 ops/entrada), bandera `itcAny` para no barrer 1549 veces una tabla ya vacia, tamano ajustable `KESTREL_JIT_ITCBITS` con nuevo defecto **14** (16384 entradas, 256 KB) elegido por A/B de pared (12 bits 2,439 s / 13 2,451 s / 14 2,418 s / 16 2,515 s). Ademas `KESTREL_JIT_STATS=<n>` fija el periodo del volcado (antes 4 M fijos, que con enlace+ITC ya no se alcanza en una tanda) y dos contadores nuevos: `[permiso]` (que limita el presupuesto de cadena: **tope 3,1 M**, o sea siempre el tope de 4096 ops) y `[icache]` (barridos/efectivos/vaciados de ITC). Retirado el atajo inseguro `KESTREL_JIT_NOINVAL`, que era solo para medir. 14/14 + prdp 3/3, regress=0 y **nodump=0** en ambos krom (mean_exact interp 88,71 / prdp 89,26), md5 sin cambio en los seis modos. |
| 2026-09-02 | gate_all | 367 s | gate_prdp | 291 s | Instrumentacion de terminadores del JIT (`[retorno] salto/completo/corto`, `[salidas] enlace/itc/lentaDirecta/lentaIndirecta`, `[cadena] rotasPorTramp`, todo detras de `KESTREL_JIT_STATS`) + resultado NEGATIVO documentado: el 84 % de los despachos del driver son bloques que acaban SIN salto (86 k / 2,25 M / 346 k), pero enlazarlos secuencialmente -- aunque baja las entradas al driver 8,8x (2,88 M -> 328 k) y sube las ops por entrada 580 -> 4902 -- MIDE PEOR: systemtest 13,27 -> 14,84 s (+12 %), y restringido a `nOps==kMaxOps` 13,58 -> 13,80 s; SM64 empata porque ahi el palo largo no es la CPU. Revertido, con el porque escrito en `compileBlock` y en `docs/PERF-CPU.md` §20.5. Corolario: el cuello del hilo de CPU no es el despacho sino el codigo generado. 14/14 + prdp 3/3, regress=0 y nodump=0 en ambos krom (mean_exact interp 88,71 / prdp 89,26), md5 sin cambio en los seis modos. |
| 2026-09-02 | gate_all | 368 s | gate_prdp | 296 s | Diagnostico de audio EN VIVO: `audio::statSnapshot` + linea `[hb] audio: silencio/descartadas/anillo (min de la ventana)` en el latido. `KESTREL_AUDIOSTAT` solo habla al cerrar, y con ventana el emulador no cierra solo (el bucle de la ventana sigue tras el tope de campos), asi que un "se oye entrecortado" no se podia localizar mientras pasaba. Medido en SM64 --play con parallel-RDP: silencio 0,15-0,54 %, descartes 0, colchon minimo por ventana 480-928 muestras (15-29 ms) -- el audio ya NO se corta con el codigo actual. Ademas el cartel de arranque decia siempre "M1: CPU interpreter" cuando el dynarec va por defecto desde hace tiempo: ahora dice "CPU dynarec" o "CPU interprete" segun `KESTREL_JIT`. 15/15 + prdp 3/3, regress=0 y nodump=0 en ambos krom, md5 sin cambio. |
| 2026-09-02 | gate_all | 347 s | gate_prdp | 302 s | Depuracion de invitado: el escaner de hilos `KESTREL_THREADSCAN` ahora lee las OSThread por `peekPhysCoherent` (la RDRAM cruda ensenaba estado de hace varios cambios de contexto: salian dos hilos en RUNNING a la vez), acepta punteros y PC guardados en el segmento mapeado por TLB (`0x7xxxxxxx`, o sea era ciego justo en los juegos con TLB) y ya no descarta `id == 0`, que es como se registran los hilos de la propia libultra (vimgr, pimgr). Ademas el volcado de hilos leia con `read32()`, que lanza la excepcion de verdad: sondear las dos bases corrompia EPC/Cause/BadVAddr del guest y metia dos **AdEL fantasma** en `[exchist]` que parecian un fallo de Perfect Dark; ahora va por `probing=true` + `peekPhysCoherent`. Y el servidor de telemetria escuchaba con `listen(sock, 1)`: una orden larga bloquea el unico hilo y las conexiones siguientes salian rechazadas (parecia que el emulador se habia caido), backlog a 8. 15/15 + prdp 3/3, regress=0 y nodump=0 en ambos krom (mean_exact interp 88,71 / prdp 89,26), md5 sin cambio. |
| 2026-09-02 | gate_all | 360 s | gate_prdp | 313 s | **RAIZ del cuelgue de Perfect Dark**: congelar el RDP (DPC_STATUS bit3) paraba el command processor pero kestrel ademas TIRABA todo DPC_END que llegase congelado, y nadie reanudaba el FIFO al limpiar FREEZE. En HW congelar no descarta nada: al descongelar el RDP sigue desde CURRENT hasta END. PD congela el RDP entre tareas graficas y su segunda tarea dejaba 128 B de comandos -- con su SYNC_FULL -- sin ejecutar: sin interrupcion DP el planificador de libultra da el RDP por ocupado para siempre, no despacha otra tarea grafica ni manda OS_SC_DONE_MSG, MAIN se queda en el bucle de 6 retrazas del copyright y con el se para joysTick (SI congelado en 569 transacciones = el mando muerto). Extraido `Memory::dpcAdvance()` y llamado tambien al limpiar FREEZE. Ahora PD encadena tareas graficas, una interrupcion DP por cuadro, pasa las 6 retrazas, intercambia buffer (origin alternando) y sigue cargando del cartucho. Herramientas nuevas para llegar ahi: `[dpwr]` (cada escritura a un registro DPC con el estado previo) y `[rsp] END` (cabecera OSTask + PC del BREAK + ciclos + ventana DPC al acabar cada tarea). Ademas hand-off de CIC-6105 en `KESTREL_LLE_IPL3` (t3=0xA4000040, ra=0xA4001550: su IPL3 lee su tabla con `lw t2,0x44(t3)` y sin t3 moria en TLBL). 15/15 + prdp 3/3, regress=0 y nodump=0 en ambos krom (mean_exact interp 88,71 / prdp 89,26), md5 sin cambio. |
| 2026-09-02 | gate_all | 357 s | gate_prdp | 294 s | Mando inyectable por telemetria (`pad.set`/`pad.get`, herramientas MCP `controller_set`/`controller_state`). La duracion de la pulsacion se cuenta en SONDEOS del joybus del mando 1, no en milisegundos: es el unico reloj que el juego percibe, asi que dura los mismos cuadros de juego al 30% o al 200% de tiempo real y el juego ve el flanco de bajada que esperan los menus. Hacia falta porque KESTREL_BUTTONS es fija (boton pisado para siempre) y escribir padButtons por mem.write lo pisa el bucle de la ventana al cuadro siguiente. Con ella se comprobo solo hasta donde llega PD tras el arreglo del FIFO: logo de Rare -> logo N64 3D -> cinematica de intro -> logo PERFECT DARK -> menu Select Location navegable. parallel-rdp limpio; SoftRDP llega al mismo sitio pero con lineas blancas en las aristas y fondo lavado (= la frontera de cobertura/AA ya documentada, ahora vista en un juego real). 15/15 + prdp 3/3, regress=0 y nodump=0 en ambos krom, md5 sin cambio. |
| 2026-09-03 | gate_all | 375 s | gate_prdp | 307 s | **Cuelgue intermitente de Perfect Dark cerrado** (se descarrilaba ~1 de cada 8 arranques desde que el enlace de bloques alcanza al codigo TLB-mapeado). Dos fallos genuinos. (1) La ranura vacia de la ITC se marcaba con `va = 0`, y 0 es un destino que el guest SI puede pedir (un `jr` con el registro a cero, que en HW toma excepcion): `itcIndex(0) == 0` hacia casar la ranura 0 y el `jmp [rdx+8]` saltaba al host 0 -- reproducido como `EXEC at host addr 0x0` con `guest pc=0`. Ahora la ranura vacia lleva la misma VA imposible que las guardas de enlace (`kNoLink = 1`, impar). (2) `kPaceSlack` seguia en 1 M, calibrado por rendimiento contra un hilo de CPU mucho mas lento: dejaba a la CPU emulada ~11 ms -- dos tercios de campo -- por delante de una tarea de RSP en vuelo, y eso el HW no lo permite (los dos relojes van acoplados por el bus). Bajada a **4096 = `jit::kGuardMaxOps`**, el unico valor que justifica el emulador (por debajo de la granularidad del dynarec el freno no puede mandar). PD: 40 arranques limpios de 40, antes ~1 de cada 8 colgaba. Coste medido en SM64/300 intercambios: prdp-jit 5,08 s vs 5,06 s (ruido, misma fidelidad 3,32 vs 3,31 campos VI/intercambio), threaded-jit 5,18 s vs 5,06 s (-2,4 %); el despenadero de las calibraciones viejas desaparecio con el enlace TLB, la curva es plana de 4 K a 1 M. Revisada tambien la guarda `rsp.running` del camino rapido del JIT: SIGUE haciendo falta (sin ella PD cuelga 1 de 16, y SM64 emite 1804 campos VI por 300 intercambios en vez de 1024 = la CPU gasta lo ganado girando). Diagnostico: el volcado `[wdog] code @` traducia la PC con `& 0x1fffffff`, que para una VA de TLB cae fuera de la RDRAM y salia todo ceros (parecia un colchon de NOPs); ahora traduce por `cpu.tlbProbePhys`. 15/15 + prdp 3/3, regress=0 y nodump=0 en ambos krom (mean_exact interp 88,71 / prdp 89,26), md5 sin cambio. |
| 2026-09-03 | gate_all | 376 s | gate_prdp | 307 s | **Descarrilamiento tardio de Perfect Dark cerrado** (SoftRDP threaded, 8e9 instrucciones: 3 de 4 tandas se iban al garete). El RDP rasterizaba DENTRO del codigo del guest: `SET_COLOR_IMAGE` con direccion arbitraria y 97727 pixeles escritos entre 0x1000 y 0x60000. El comando malo caia siempre justo detras del DPC_END vigente, en un tramo del buffer de comandos ANTERIOR: al instalar el juego un START fresco, el worker seguia con tramos del buffer viejo encolados y leia comandos que el microcodigo ya habia reescrito. Primer intento = drenar la cola en el START fresco: curaba (0/3) pero **costaba 13-15 % de pared** (SM64/200 intercambios threaded-jit 3,57 -> 4,10 s, prdp-jit 3,56 -> 4,09 s) porque serializa RSP y RDP, y ademas alejaba la fidelidad (campos VI/intercambio 4,05 -> 3,31, oraculo lockstep 4,09). Arreglo definitivo: **el consumidor lee una COPIA del tramo de comandos** (`Memory::rdpSnapshot`, tomada al encolar -- instante de lectura que el HW tambien puede elegir, y el productor ya habia escrito esos bytes antes del kick), con **dos generaciones alternas** para que el buffer nuevo no pise la copia del viejo sin consumir; dentro de una misma generacion el propio juego protege el anillo con su flow-control sobre DPC_CURRENT, que ya publicamos honesto (`rdpPublishCurrent`). PD 0 malas de 4, pared 3,55 s (threaded-jit) y 3,54 s (prdp-jit) = el coste del drenado desaparece entero, y campos VI/intercambio 4,08 ≈ oraculo. Instrumental nuevo que llevo hasta ahi: `KESTREL_RDPGUARD=<lo>:<hi>`, `KESTREL_CIFLOOR=<phys>` y `KESTREL_WRTAG=1` (ultimo escritor por bloque de 16 B + ventanas de FIFO sin consumir). OJO metodologico: `KESTREL_WRTAG` frena a la CPU respecto al RDP y ESCONDE la carrera (parecia 1/3 en vez de 3/4) -- todo A/B de esto va sin instrumentar. 15/15 + prdp 3/3, regress=0 y nodump=0 en ambos krom (mean_exact interp 88,71 / prdp 89,26), md5 sin cambio. |
| 2026-09-04 | gate_all | 356 s | gate_prdp | 370 s | **Los cuatro mandos**: joybus sirve los canales 0-3 (antes solo el 0), accesorio POR PUERTO con Controller Pak propio (`.mpk`, `.mpk2`..`.mpk4`) y **Rumble Pak** real (sin RAM, ventana 0x8000-0x8FFF = 0x80, escritura a 0xC000 mueve el motor; ranura vacia = CRC de datos invertido). Entrada de los cuatro puertos con aparato elegible (teclado / un mando concreto de Windows / el mixto de siempre en el puerto 1), identidad guardada por NOMBRE porque Windows renumera los joysticks, reintentada en cada barrido de 0,5 s (enchufar a medio juego reclama el puerto solo). Vibracion por XInput cargado a mano, solo en el flanco. Perfil y lanzador con un mapeo por conector (`pad`, `pad2`..`pad4`) + `KESTREL_PAD1..4` / `PADDEV1..4` / `PADS` / `PADACC`. Dialogo rehecho: selector Mando 1-4, Conectado, Accesorio y Aparato con la lista viva de mandos de Windows, y el mando de N64 dibujado en GDI y clicable. 15/15 + prdp 3/3, regress=0 y nodump=0 en ambos krom (mean_exact interp 88,71 / prdp 89,26), md5 sin cambio en los siete modos. |
| 2026-09-04 | gate_all | 365 s | gate_prdp | 299 s | **Pantallas en negro**: (1) `osTvType`/RDRAM 0x300 salen ahora del byte de pais de la cabecera (PD europeo se arrancaba como NTSC y `mainInit()` giraba), y con el la tasa de campos y el reloj de video que fija el DAC del AI. (2) **CIC-NUS-6105**: su IPL3 hace dos cosas que ningun otro hace y que los juegos comprueban — copia su propia imagen de DMEM 0x554..0x888 a RDRAM 0x004..0x338 (sesgo -0x550; de ahi sale el `*(0xA00002E8)==0xC86E2000` que PD exige) y arranca el RSP con un microcodigo que descifra por XOR del residuo de IPL2 que deja la PIF ROM, cuyo unico efecto es un DMA de escritura con paso (SP_DRAM_ADDR=0x2FB1F0, SP_WR_LEN=0xFE817000: 24 filas de 8 B cada 0xFF0) sembrado desde RDRAM 0x1E8; DK64 comprueba la fila 3 (`*(0xA02FE1C0)==0xAD170014`). El microcodigo no se puede ejecutar sin PIF ROM pero su efecto es fijo, asi que se reproduce para CUALQUIER cartucho 6105, tambien bajo `KESTREL_LLE_IPL3`; con eso muere el ultimo hardcode a un juego del arranque (la palabra 0x2E8 de PD). Ademas RDRAM 0x3F0 = tamano de RDRAM (donde la autoprueba de la PIF lo deja y de donde el IPL3 6105 saca `osMemSize`). **Donkey Kong 64 arranca y renderiza la intro de las lianas en 3D**; PD renderiza sin hardcode. 15/15 + prdp 3/3, regress=0 y nodump=0 en ambos krom (mean_exact interp 88,71 / prdp 89,26), md5 sin cambio en los siete modos. |
| 2026-09-04 | gate_all | 414 s | gate_prdp | 340 s | **Particularidades de cada CIC-NUS**. El arranque rapido identifica el chip por el CRC32 de la imagen IPL3 del cartucho y de ahi salen cuatro cosas: la semilla (0x3F 6101/6102/7101/7102, 0x78 6103/7103, 0x91 6105/7105, 0x85 6106/7106, 0xAC 5101), la direccion de arranque (7102 la ignora y va fija a 0x80000480; 6103/7103 y 5101 restan 1 MB a la de la cabecera y 6106/7106 restan 2 MB, **tanto al destino del DMA del primer megabyte como al salto**, porque el IPL3 lleva una sola direccion en un solo registro), el `osCicId` en la variante de region correcta (RDRAM 0x310) y el estado de registros con el que el IPL3 entrega el control. Ese estado no es inicializacion sino el residuo del checksum del primer megabyte, o sea que depende del chip Y de la region (para 6102 el checksum del IPL2 es 0xA536C0F1D859 y el arranque deja a0=0xA536 y, en PAL, a1=0xC0F1D859): tabla completa v0/v1/a0/a1/at/t4/t5/t6/t7/t9 por CIC+region, mas s7=osVersion (6 PAL / 0 NTSC), t3=0xA4000040 y ra=0xA4001550 NTSC / 0xA4001554 PAL. NTSC vs PAL **no** sale del CRC (6102/7101, 6103/7103, 6105/7105 y 6106/7106 comparten imagen byte a byte) sino del byte de pais. Anadido tambien el residuo del 6105 en IMEM 0x004 (0x8DA807FC NTSC / 0xBDA807FC PAL, distinto por region porque la PIF ROM PAL es otra imagen). Fuentes: tabla CIC-NUS y PIF-NUS de n64brew, `cic/cic.cpp` de ares, `CRegisters::Reset` de Project64. Se decidio NO emular la escritura de region+semillas en PIF RAM 0x7E4: el `memSwapSecrets()` de la PIF muestra que el HW las vuelve a esconder antes de que corra el IPL3. 15/15 + prdp 3/3, regress=0 y nodump=0 en ambos krom (mean_exact interp 88,71 / prdp 89,26), md5 sin cambio en los siete modos. Pared mas alta de lo normal (414/340 vs 334-376/289-370) por carga del anfitrion, no por el cambio: krom interp 262 s vs 222 s en la misma tanda de la mañana con el mismo exe. |
| 2026-09-04 | gate_all | 414 s | gate_prdp | 340 s | **dcMiss (fusion volcado+relleno del fallo de D-cache) + lote de validacion mudo** (`KESTREL_AUDIO=0` por defecto en `scripts/validate.py`). 15/15 + prdp 3/3, md5 sm64 sin cambio en los siete modos (`466282775dbd0ac084946558a1c30771`) y en prdp (`b5521b24d8fc280fbf102df22d7d30cb`), krom interp 371/371 mean_exact 88,71 y prdp 371/371 mean_exact 89,26, regress=0 y nodump=0 en ambos. El A/B de dcMiss no es medible en este anfitrion (base min 5112 ms vs 5125 ms en 4 rondas intercaladas, empate dentro del ruido); se conserva porque hace estrictamente menos trabajo por fallo (una llamada, una resolucion de linea y de puntero/tamano de RDRAM en vez de dos). |
| 2026-09-04 | gate_all | 410 s | gate_prdp | (no suelta) | **Motor de trucos GameShark** (`src/core/cheats.{hpp,cpp}`): familias 80/81 (escritura por campo, cacheada), A0/A1 (parche unico de arranque, sin pasar por la D-cache), D0-D3 (condiciones) y el repetidor 50. El ritmo es el CAMPO DE VIDEO porque el aparato real colgaba de la interrupcion del VI, y el nibble alto de la direccion decide la ruta de escritura (KSEG0 = por D-cache, KSEG1 = directa a RDRAM); 88/89 (boton fisico del cartucho) y CC/DE/EE/FF (control interno) se reconocen y se avisa de que no se aplican. Fichero `.cht` al lado de la ROM o `KESTREL_CHEATS`, con opcion en lanzador y menu. Test propio `test/cheat_test.cpp`. 15/15, regress=0 y nodump=0, md5 sin cambio en los siete modos. La puerta GPU no se corrio suelta para este cambio: la cubre la fila siguiente, que lleva el mismo codigo dentro. |
| 2026-09-04 | gate_all | 392 s | gate_prdp | 342 s | **ROMs comprimidas (.zip, .gz) + los cuatro tests unitarios dentro de la puerta**. `src/core/archive.{hpp,cpp}`: DEFLATE propio (RFC 1951, ~200 lineas, forma compacta de la RFC sin tablas de busqueda) en vez de zlib, porque el `.exe` que se publica va con `-DKESTREL_STATIC=ON` y meter una dependencia por un descompresor que corre UNA vez al abrir la ROM no sale a cuenta. Zip (metodos 0 y 8) y gzip (FEXTRA/FNAME/FCOMMENT/FHCRC), CRC-32 comprobado en los dos; dentro del zip se elige la entrada con extension de ROM y si no la mas grande; 7z y rar se rechazan CON SU NOMBRE. Se desempaqueta ANTES de normalizar el orden de bytes (`Rom::loadFile`), que es lo unico correcto: lo que hay que reconocer como z64/n64/v64 es la ROM de dentro. Los ficheros que viven al lado (`.eep/.sra/.fla/.mpk`, `.st0`-`.st9`, `.cht`) cuelgan del nombre SIN la extension del contenedor (`stripContainerExt`), asi que `mario.z64.gz` y `mario.z64` comparten partida. Verificado extremo a extremo: SM64 arrancado 60 campos desde SIETE envoltorios (zip deflate, zip guardado, zip de tres entradas, zip con la ROM en `.v64`, gzip de un `.n64`, gzip de un `.z64`, `.n64` crudo) = los siete dan `466282775dbd0ac084946558a1c30771`. Ademas se rescataron `rsp_test` (dos podredumbres: `sp_status` es `std::atomic` desde que el RSP tiene hilo, y en Lockstep escribir CLEAR_HALT solo ARMA el nucleo) y `save_test` (Controller Pak mudado a `padPort[i]`), y `gate_all.sh` compila y ejecuta ahora los CUATRO (`rsp_test`, `save_test`, `cheat_test`, `archive_test`) antes de nada: se pudrieron precisamente porque ninguna puerta los miraba. 15/15 + prdp 3/3, krom interp 371/371 mean_exact 88,71 y prdp 371/371 mean_exact 89,26, regress=0 y nodump=0 en ambos, md5 sin cambio en los siete modos y en prdp (`b5521b24d8fc280fbf102df22d7d30cb`). |
| 2026-09-04 | gate_all | 406 s | gate_prdp | 342 s | **Escalado interno (2x/4x/8x) + relacion de aspecto**, hueco P1 #6 de `docs/GAPS.md` (salvo texturas HD, que son otro canal). El escalado es una BANDERA DEL CONSTRUCTOR de parallel-rdp (`COMMAND_PROCESSOR_FLAG_UPSCALING_{2,4,8}X_BIT`): dimensiona los buferes de RDRAM ampliada que crea el `CommandProcessor`, asi que NO puede cambiar en caliente y la opcion va marcada `(*)` (relanzar). `KESTREL_SSAA` (supermuestreo al resolver de vuelta a la RDRAM 1x del invitado, no un reescalado del scanout) solo se activa con factor > 1 porque `Renderer::init_renderer` lo rechaza a 1x. El volcado de framebuffer NO sirve de prueba -- lee la RDRAM del invitado, que es 1x por definicion -- asi que la evidencia es el nuevo log `[vrdp] scanout WxH`: SM64 da 640x240 / 1280x480 / 2560x960 / 5120x1920 para 1/2/4/8. El aspecto sale de `rt::aspectW/H` (`KESTREL_ASPECT`, en caliente desde el menu): 4:3 de fabrica porque el VI saca SIEMPRE senal 4:3 pase lo que pase la resolucion del framebuffer, 16:9 para los juegos que dibujan anamorfico en su modo panoramico (no ensancha el campo de vision: eso solo lo puede hacer el juego), 0:0 = estirar. Por defecto banderas=0 y el mismo rectangulo de destino de siempre, o sea cero efecto sobre las puertas. Doc `docs/ESCALADO-Y-ASPECTO.md`. 15/15 + prdp 3/3, krom interp 371/371 mean_exact 88,71 y prdp 371/371 mean_exact 89,26, regress=0 y nodump=0 en ambos, md5 sin cambio en los siete modos (`466282775dbd0ac084946558a1c30771`) y en prdp (`b5521b24d8fc280fbf102df22d7d30cb`). |
| 2026-09-04 | gate_all | 404 s | gate_prdp | 326 s | **El paquete lleva los DOS rasterizadores** (`scripts/release.sh` compila ahora tambien `build-static`, SoftRDP estatico; `pack.sh` lo pasa por `SOFTBUILD` y `dist.sh` lo copia como `kestrel64-soft.exe` con su nota `kestrel64-soft.build` y su pasada de `ldd`; el lanzador lo detecta en modo instalado, asi que la opcion "Rasterizador" sigue siendo una eleccion de verdad fuera del arbol de fuentes; el `.iss` no cambia porque mete `dist\*` entero). Empaquetar ese .exe destapo un fallo REAL del emulador que no se veia porque nadie ejecutaba un binario sin parallel-RDP fuera de las puertas: `System::runLoop` daba por bueno el default "GPU si" aunque el backend NO estuviera compilado, con lo que `vrdpWaitReady` giraba sus 15 s completos y despues **no abria la ventana** -- doble clic en `kestrel64-soft.exe` = quince segundos parado y nada. Arreglado donde toca: `vrdp::built` (constante de compilacion, distingue "no lo lleva" de "lo lleva y aun no levanta"), `vrdpWaitReady` sale al instante si no lo lleva, `prdpWanted = vrdp::built && ...`, y un aviso si alguien pide `KESTREL_PRDP=1` en el .exe que no lo tiene. De paso `KESTREL_VIDEO`/`KESTREL_NOVIDEO` se leen por VALOR y no por presencia: `KESTREL_VIDEO=0` encendia la ventana. Medido: SoftRDP con ventana arranca en 4 s (antes 15 s y sin ventana) y sin ventana sale en 1 s. 15/15 + prdp 3/3, cuatro tests unitarios ALL PASS, krom interp 371/371 mean_exact 88,71 y prdp 371/371 mean_exact 89,26, regress=0 y nodump=0 en ambos, md5 sin cambio en los siete modos (`466282775dbd0ac084946558a1c30771`) y en prdp (`b5521b24d8fc280fbf102df22d7d30cb`). |
| 2026-09-04 | gate_all | 450 s | gate_prdp | 396 s | **Las tres esperas del hilo de CPU sobre los workers del RCP dejan de ser mudas** (`rspAwaitIdle`, la de `rspSubmitKick` y `rdpDrain` iban a `cv.wait` pelado: un worker que dejara de publicar congelaba el emulador para siempre y sin decir nada, indistinguible de lentitud). No se puede abandonar la espera -- seguir sin el resultado corrompe el estado del invitado y el hardware tampoco se cansa -- asi que el tope no la rompe sino que la PARTE: `awaitReporting` espera en rondas de `KESTREL_RCPWAIT` ms (2000 por defecto, `=0` vuelve a la espera muda) y en cada vuelta imprime el estado del dominio: del RSP ocupado/kick/corriendo, `sp_status`, `sp_pc`, PC de IMEM, ciclos publicados y tipo de OSTask; del RDP cola, ocupado y `dpc start/cur/end/status`. Los dos partes se dispararon a proposito y se leyeron en vivo (el del RSP con el PC de IMEM avanzando durante una tarea larga, el del RDP con `dpc cur` avanzando), que es justo la distincion que hace falta: si avanza es tarea larga, si esta clavado es el cuelgue de verdad. Ademas el manifiesto `VERSION.txt` lo escribe ahora `dist.sh` ANTES de cerrar el zip, en vez de `release.sh` despues: escrito despues, el portable viajaba sin el y el instalador recogia el del paquete anterior. 15/15 + prdp 3/3, cuatro tests unitarios ALL PASS, krom interp 371/371 mean_exact 88,71 y prdp 371/371 mean_exact 89,26, regress=0 y nodump=0 en ambos, md5 sin cambio en los siete modos (`466282775dbd0ac084946558a1c30771`) y en prdp (`b5521b24d8fc280fbf102df22d7d30cb`). Pared alta (450/396 vs 356-414/289-370) por carga del anfitrion, no por el cambio: el cambio solo anade un `wait_for` en lugar de un `wait` en tres esperas que en una tanda sana no llegan a agotar ni una ronda. |
| 2026-09-04 | gate_all | 415 s | gate_prdp | 349 s | **Consola de serie de verdad: 4 MB de RDRAM** (`KESTREL_RDRAM=4|8`, opcion "Memoria RDRAM" del lanzador, grupo Cartucho, con relanzado obligatorio). `Memory::reset(bool)` ya sabia reservar 4 u 8 MB y `CPU::fastBoot` ya copiaba `rdram.size()` a las dos ventanas por las que pregunta el invitado (`osMemSize` en RDRAM 0x318 y la copia de 0x3F0 que deja la autoprueba de la PIF), pero `System::init` llamaba SIEMPRE con `expansionPak=true`: la mitad baja del parque nunca se habia visto como se ve sin el cacharro. No puede cambiarse en caliente -- el tamano fija el suelo del mapa de memoria del invitado (heap, framebuffers, tablas) al arrancar -- y el arranque lo dice ahora en el log (`[system] RDRAM 4 MB (consola de serie)`). La prueba es del INVITADO, no del emulador: SM64 a 4 MB da el md5 de referencia intacto (`466282775dbd0ac084946558a1c30771`, no pidio nunca el Expansion Pak) y Donkey Kong 64 a 4 MB da OTRO fotograma (`0d05190dc6efd189dcee266cc108827a` frente a `5683d22e66c393d50602b648b1ec660d` a 8 MB) en el que se lee "N64 EXPANSION PAK NOT INSTALLED": el numero llega al codigo del juego y le cambia el comportamiento. Las lecturas fisicas pasadas el final de la RDRAM ya devolvian 0 y `jitRdramSz` ya se toma del tamano real por compilacion, las dos con casos propios de 4 MB en `wildmem_test`; la cabecera del savestate lleva `rdramSize` y rechaza cargar en una maquina de otro tamano. 15/15 + prdp 3/3, cinco tests unitarios ALL PASS, krom interp 371/371 mean_exact 88,71 y prdp 371/371 mean_exact 89,26, regress=0 y nodump=0 en ambos, md5 sin cambio en los siete modos (`466282775dbd0ac084946558a1c30771`) y en prdp (`b5521b24d8fc280fbf102df22d7d30cb`). |
| 2026-09-04 | gate_all | 409 s | gate_prdp | 352 s | **`KESTREL_EXCODD` mira el estado del TLB, no el numero de excepcion.** El volcado de "excepcion imposible" (contexto + las 8 palabras que hay REALMENTE en la direccion que fallo + los 32 GPR) filtraba por codigo: `excCode > 3 && != 8`, o sea fuera Int, TLBMod, TLBL, TLBS y Syscall. Meter TLBL/TLBS a secas seria falso -- un juego que use el TLB los toma a punta pala -- pero eso depende del ESTADO: si no hay NI UNA entrada con el bit V puesto, ninguna traduccion mapeada puede acertar jamas y un TLBL deja de ser paginacion bajo demanda para ser un puntero que se fue a kuseg. Nuevo `CPU::tlbAnyValid()` (barrido de 32 entradas que sale al primer V, y solo se llama en excepciones de TLB) y el filtro pasa a `(excCode > 3 && != 8) || ((excCode == 2 || 3) && !tlbAnyValid())`. Comprobado en los DOS sentidos: `wildmem_test` con `KESTREL_EXCODD=4` pasa de cero volcados de TLB a cuatro de code=2 y dos de code=3 (sus casos de kuseg/ksseg sin mapear, TLB virgen), y SM64 y DK64 a 120 campos no ganan ni un aviso (siguen con sus mismos code=11). Camino de diagnostico puro: apagado no cuesta nada y no toca la semantica. 15/15 + prdp 3/3, cinco tests unitarios ALL PASS, krom interp 371/371 mean_exact 88,71 y prdp 371/371 mean_exact 89,26, regress=0 y nodump=0 en ambos, md5 sin cambio en los siete modos (`466282775dbd0ac084946558a1c30771`) y en prdp (`b5521b24d8fc280fbf102df22d7d30cb`). |
| 2026-09-04 | gate_all | 431 s | gate_prdp | 349 s | **Peliculas de entrada (TAS) + avance por fotogramas**, hueco P2 #12 de `docs/GAPS.md`. Lo que se graba NO es lo que aprieta el jugador sino **lo que el juego LEE**: cada respuesta al comando 0x01 del joybus (botones+ejes) de cada conector, enganchada en `Memory` DESPUES del mando del anfitrion y del inyectado por telemetria. Esa es la unica frontera que el invitado percibe, asi que la pelicula es independiente de la velocidad, del rasterizador y del modo de CPU; grabar por campo de video seria mentira (hay juegos que sondean dos veces en un campo y otros que se saltan campos, y el orden de los cuatro conectores lo decide el bloque de comandos que el juego escribe en la PIF RAM). Formato `.k64m` legible con `xxd`: cabecera de 64 B (magico, version, CRC1/CRC2 del cartucho, norma de TV, RDRAM en MB, mascara de conectores, numero de muestras) + muestras planas de 5 B (conector, botones alto/bajo, eje X, eje Y). Modulo `src/core/movie.hpp` de solo cabecera, con `KESTREL_MOVIE_REC` / `KESTREL_MOVIE_PLAY`, opciones de lanzador y `docs/TAS.md`. El **avance por fotogramas** toma como cuanto el CAMPO DE VIDEO (`fieldClosed`), que es donde el juego sondea el mando, o sea un avance = una muestra de pelicula: `System::stepFields` deja correr al bucle AUNQUE este en pausa, con teclas P / F / Shift+F y orden de telemetria `frame.advance` (bloqueante con tope, sin retener `coreMutex`) y su envoltorio MCP. Un estado guardado rebobina el JUEGO, asi que la cinta le sigue: seccion `MOVI` con el contador de sondeos y `movie::seek`, **version de estado 4 -> 5**. Verificado y DISCRIMINANTE en SM64 a 600 campos con `KESTREL_BUTTONS=0x1000`: control `109c2277d34a411b52ddcc467cb1ad12` != grabacion `0ce65ed4eccc8533de892db5d7d1bf44`, y la repeticion reproduce EXACTO el md5 de la grabacion con las mismas 471 muestras (a 60 campos las tres coincidian: SM64 ignora START tan pronto, o sea que el primer A/B no probaba nada). Cartucho cruzado rechazado con los dos CRC (pelicula de SM64 sobre DK64), avance exacto por telemetria (1->1, 5->5, nucleo quieto entremedias) y rebobinado del estado comprobado con el contador de `emu.status` (54 -> 74 -> 54). La autorrevision del cambio saco tres fallos reales, arreglados: la reproduccion no respetaba la cuenta de la cabecera (regrabar encima dejaba leer la cola de la toma anterior), la grabacion truncaba en silencio si fallaba `fwrite` (ahora avisa, cierra y para) y `polls`/`ended` no eran atomicos aunque `emu.status` los lee desde el hilo de telemetria. 15/15 + prdp 3/3, cinco tests unitarios ALL PASS, krom interp 371/371 mean_exact 88,71 y prdp 371/371 mean_exact 89,26, regress=0 y nodump=0 en ambos, md5 sin cambio en los siete modos (`466282775dbd0ac084946558a1c30771`) y en prdp (`b5521b24d8fc280fbf102df22d7d30cb`). OJO metodologico de esta tanda: una puerta MATADA a mitad deja el log lleno de `REGRESS ... -> nan` y `NODUMP rc=3221225794` (0xC0000142, proceso abortado al arrancar); no es regresion, es el `taskkill`. Solo cuenta una tanda que termina sola. |
| 2026-09-04 | gate_all | 405 s | gate_prdp | 346 s | **Rebobinado**, hueco P2 #11 de `docs/GAPS.md`. Una sola foto viva del estado entero mas una pila de **diferencias hacia atras**: cada entrada dice lo que hay que reescribir sobre la foto de ahora para recuperar la de antes, que es la direccion en la que se rebobina y la barata (dos campos seguidos difieren en poco, no en los 8 MB de la RDRAM). Guardar fotos enteras serian ~4 GB cada cuatro segundos y comprimir 8 MB por campo cuesta mas que el campo. Piezas: `savestate.cpp` gana el par en memoria `captureState`/`restoreState` (mismo visitante, sin fichero) y `src/core/rewind.{hpp,cpp}` el codec (`u64 oldLen`, luego registros `u32 iguales · u32 distintos · <bytes viejos>`, comparacion por palabra de 4 B) mas el motor; tecla de retroceso mientras se mantenga apretada (no por flanco), `rewind.step` por telemetria (MCP `rewind_step`), `status` publica `rewind{steps,bytes,interval,budget}` y tres casillas en el lanzador. La foto se toma con el RCP quieto (`System::quiesceRcp`: drenar RDP, terminar tarea de RSP) para que un estado tomado en Lockstep cargue en Threaded, y cargar un estado guardado tira la cinta. **Apagado de fabrica** porque cuesta: medido en SM64/200 intercambios, apagado 4487/4315 ms, foto cada 2 campos 6221/6563 ms (**+45 %**), cada 6 campos 5526/5420 ms (**+24 %**); apagado no es mas que comparar un contador contra cero por campo. Cinta: ~79 KB por paso, 256 MB ≈ 3400 pasos ≈ 2 min de pantalla de titulo. Carrera real encontrada y arreglada de camino: el bucle VACIA el buzon de peticiones ANTES de hacer el trabajo y publicar `stateMsg`, o sea que un cliente que esperaba a que el buzon quedara vacio leia el parte ANTERIOR (`rewind.step` decia fallo con error vacio mientras el log decia `rebobinado 20 (quedan 99)`); ahora hay `System::stateSeq`, un contador que sube DESPUES de escribir el parte, y `state.save`/`state.load`/`rewind.step` esperan a que cambie -- los dos primeros tenian el mismo fallo latente. Verificado: `test/rewind_test.cpp` (nuevo, sexto test unitario de `gate_all`: casos limite + 1000 pares al azar, delta = 16,4 % de la foto, y diferencia truncada rechazada) y extremo a extremo en SM64 -- campo 200 `ba62569b8d8ae560a61423e9061ba380` ≠ campo 240 `3cadb6c96d09a9a498501ee2f381cb9d`, rebobinar 20 pasos vuelve al primero y volver a correr 40 campos da otra vez el segundo byte a byte, que es la prueba que vale: si algo del estado hubiera quedado viejo, el FUTURO que sale de el seria distinto aunque la foto pareciera igual. Detalle en `docs/REWIND.md`. |
| 2026-09-04 | gate_all | 378 s | gate_prdp | 314 s | **CPI como factor explicito** (`KESTREL_CPI`), primer hueco de fidelidad de temporizacion de `docs/GAPS.md`. Hasta ahora el emulador ataba "instruccion retirada" a "tick de COP0 Count"; como Count corre a medio reloj de CPU, eso clava el modelo entero en CPI 2 y de ahi salen las 782 032 instrucciones por campo NTSC (937 488 en PAL). El VR4300 real anda por 1,2-1,4. Ahora el ratio es un numero: `cpi256 = 128 x CPI` = ticks de Count x256 por op, con resto acumulado (`countFrac`) para no perder nada en la division; **256 = de fabrica = un tick por op = exactamente lo de siempre**, y por eso esta tanda sale identica byte a byte (md5 `466282775dbd0ac084946558a1c30771` en los siete modos, `b5521b24d8fc280fbf102df22d7d30cb` en prdp, krom regress=0 improve=0 new=0 nodump=0 en interp y prdp). Se lee UNA sola vez (`CPU::cpiFromEnv`) y de ese unico numero cuelgan las DOS cosas que dependen de el: el ritmo de Count dentro de la CPU y `clocks.cyclesPerInsn` -> `fieldInsns()`, o sea el presupuesto de instrucciones por campo del bucle de System y de la lectura de `VI_V_CURRENT`. Derivar la segunda de la primera (en vez de volver a leer la variable de entorno) es lo que impide repetir el fallo que ya documentaba `Clocks::cyclesPerInsn`: dos relojes distintos en el mismo emulador. Cota dura `cpi256 <= 256`: todas las guardas de borde de timer del JIT comparan la distancia a Compare contra el numero de OPS del bloque, y solo siguen siendo conservadoras mientras `ticks(ops) <= ops` -- con menos ticks devuelven el control de mas (coste), nunca de menos (interrupcion perdida). Sitios enrutados por `countTicks()`: interprete, commit diferido de la cadena del JIT, salida de bloque, commit de eslabones pendientes y el arnes de brdiff. `countFrac` es estado de invitado -> savestate **version 6** y guardado/restaurado en los arneses de diff. Unico sacrificio: el atajo de `mfc0 Count` dentro del bloque (jit.cpp:780) emitia "entrada + idx ops", exacto solo con un tick por op; con el factor movido cede al interprete en vez de aproximar. Lo que NO escala, a proposito: `Random` y el decaimiento del pestillo del PI siguen en instrucciones (en HW van por ciclo) porque su comportamiento actual es el que fijan los tests TLB/Random. Medido ademas con el factor MOVIDO: n64-systemtest pasa igual con CPI 1,5, 1,4 y 1,25 en `threaded` y `threaded-jit` (`Base 0/3721 Timing 0/2 Cycle 0/6`), y el md5 de SM64 a 60 campos no cambia ni entre CPI ni entre interprete y JIT -- luego los tests Timing/Cycle no bloquean el cambio pero TAMPOCO lo deciden (no distinguen 2 de 1,25). El defecto se queda en 2 hasta tener la medida en juegos. |
| 2026-09-08 | gate_all | 367 s | gate_prdp | 337 s | **Patron de tomas de cobertura del SoftRDP = el del hardware, y la lectura del mando de DK64 medida.** Las ocho sub-muestras no van en las mismas dos columnas: el RDP las ESCALONA fila a fila (en octavos de pixel, filas pares 0 y 4, impares 2 y 6). Dos oraculos independientes coinciden byte a byte -- `compute_coverage()` de parallel-rdp (`xshift = u16x4(0,4,2,6) + (x<<3)`) y `raster_coverage.c` de PixelMechanic0/softrdp (MIT, clonado en `../softrdp-ref`). Teniamos (0,25/0,75) en las cuatro filas. La suite krom NO se mueve ni una decima (`mean_exact` 88,71 y `mean_close` 92,03 clavados, regress=0, perfect=148) porque `cvg` solo se consume con antialias encendido y los tests de triangulo de krom van con AA apagado; SM64 SI se mueve, `466282775dbd0ac084946558a1c30771` -> **`195a464c80e6216af78121fd24242f3e`**, el MISMO md5 en los siete modos. Se aislo la causa dejando la tabla uniforme: eso reproduce el md5 viejo exacto. Y el diff de los dos vuelcos dice que es graduado de borde y no corrupcion: 3.768 pixeles (4,9 % del cuadro), delta medio 24,8 con la moda en 8 (= un LSB de un canal RGB565) y **gradiente horizontal medio 50,1 en los pixeles cambiados frente a 7,7 en el resto**. Baseline de SM64 re-bendecida; PRDP intacto (`b5521b24d8fc280fbf102df22d7d30cb`, krom prdp 89,26 regress=0), que es lo esperado porque el cambio es del rasterizador de CPU. Via cerrada con numero, para que nadie la repita: aplicar aqui las otras dos reglas del hardware (cobertura cero = el pixel no existe; con AA apagado decide una sola toma) EMPEORA, porque ya las aplicamos en el recorte del tramo (`xs = ceil(xLeft)`) -- `RSPPlotTriangle` 100,000 -> 99,740 y `Cycle1FillZBufferTriangle` 98,150 -> 97,950. Ademas, hipotesis del usuario sobre la intro de DK64 («falla la liana por la lectura del mando») MEDIDA: el corte de `[frames]` cuenta ahora las lecturas del mando (`padPolls`), y salen ~= a los SYNC_FULL del RDP (1060 vs 1074), o sea **una lectura por cuadro dibujado**, que es lo correcto; lo que falta son cuadros (0,11-0,42 lecturas por campo contra 0,5 en hardware a 30 fps), y el A/B de CPI lo confirma (CPI 2 -> 0,201, CPI 1,4 -> 0,241). La causa es el presupuesto de CPU por campo, no el joybus. 15/15 + prdp 3/3, seis tests unitarios ALL PASS, krom interp 371/371 y prdp 371/371 con regress=0 y nodump=0 en ambos. |
| 2026-09-08 | gate_all | 337 s | gate_prdp | 335 s | **El defecto de CPI pasa de 2 a 1,4** (`kCpiDefault256 = 179` en `src/cpu/cpu.hpp`, razonamiento largo en `CPU::cpiFromEnv`). El 2 no era una medida: salia de que el emulador ataba "instruccion retirada" a "tick de COP0 Count" y Count corre a medio reloj, o sea que el CPI 2 era un artefacto de implementacion que le daba al juego LA MITAD del presupuesto de la consola. El VR4300 real anda por 1,2-1,4 en codigo de juego. El 2026-09-04 se dejo el factor escrito pero el defecto quieto, con tres condiciones puestas por escrito para moverlo; esta tanda las cumple las tres. (1) Mas juegos: SM64 da campos por intercambio en arranque 3,41 con CPI 2, 2,84 con 1,5, **2,76 con 1,4** y 2,69 con 1,25 = rendimiento decreciente por debajo de 1,4, el mismo patron que DK64 (5,5 -> 2,9 en arranque, 2,00 clavado en regimen estable con cualquier CPI porque ahi el juego ya llega a sus 30 fps y lo que sube es el ocio, 62 % -> 76 %). (2) Audio, que era el riesgo real porque el drenaje del AI convierte instrucciones retiradas en segundos de invitado: con 1,4 `KESTREL_AUDIOSTAT` da `silencio=0 (0.00%)` y `cortas=0`, igual que con 2; el colchon minimo del anillo se estrecha de 9504 a 3232 muestras de 22050 pero no pasa hambre. (3) El numero: 1,4 es la parte ALTA del rango real, que es lo honesto cuando las dos medidas propias (DK64 1,19, PD 1,45) son COTAS SUPERIORES de agresividad -- solo se resta el giro ocioso de libultra y todo otro busy-wait sigue contando como trabajo. Sigue siendo una aproximacion de un solo numero a algo que en el silicio depende de fallos de cache y del mix de instrucciones: no es un modelo de ciclos, es un presupuesto menos falso que el 2. `KESTREL_CPI=2` restaura el modelo historico bit a bit y `cpiIsHistoric()` (antes `cpi256IsDefault()`) pasa a querer decir eso mismo. 15/15 + prdp 3/3 con `Base 0/3721 Timing 0/2 Cycle 0/6`, md5 de SM64 sin cambio en los siete modos (`195a464c80e6216af78121fd24242f3e`) y en prdp (`b5521b24d8fc280fbf102df22d7d30cb`) -- no cambia porque la puerta corta por INTERCAMBIOS de buffer, que es estado del juego, no por cuenta de instrucciones. krom interp 371/371 `mean_exact` 88,72 y prdp 371/371 89,27, **regress=0 improve=2** en los dos: las dos mejoras son `CubeFillTriangle` 16BPP y 32BPP (53,10 -> 55,28), que son de los tests ANIMADOS -- el CPI cambia cuantas instrucciones caben en un campo y con ello la FASE de la animacion, no la exactitud del rasterizador; por eso se re-bendice la baseline de INTERP (16 lineas, todas de tests animados, ninguna a peor) y NO la de PRDP: alli la misma orden reescribe 101 lineas y entre ellas mete valores a PEOR (`HelloWorldRDP16BPP` 100,00 -> 99,06), o sea que bendecir bajaria el liston con ruido de la GPU (latencia de sumision, divergencia PRDP ya documentada). La baseline vieja de prdp se queda y su `improve=2` es informacion, no deuda. |
| 2026-09-08 | gate_all | 420 s | gate_prdp | 330 s | **La transaccion del SI ya no es instantanea.** `siDma()` factura el tiempo de la linea joybus leyendo el bloque de ordenes de verdad (4 us/bit ⇒ 32 us/byte, +3 us de parada de consola por orden transmitida, +4 us de parada del mando por orden que responde, +5 us por mover los 64 B RDRAM↔PIF RAM), deja `SI_STATUS=DMA_BUSY` y arma un plazo `siDoneAt` en el reloj de invitado; `siFinish()` copia a RDRAM y levanta `MI_SI` al vencer. Determinista en los siete modos porque vence contra `cpu.retired`: el interprete lo mira por instruccion, el JIT se niega a compilar el bloque que se lo tragaria (`siDueIn()` en `jitTryBlock`, como el borde de `Count`==`Compare`) y recorta el permiso de encadenado. Foto de estado v6→v7. De paso, escribir `SI_STATUS` ahora solo borra la interrupcion en vez de poner el registro a 0: antes borraba el bit de ocupado con la transaccion en vuelo. A/B `KESTREL_SIINSTANT=1`. 15/15 + prdp 3/3, md5 de SM64 sin cambio en los siete modos y en prdp, krom regress=0 en ambos (mean_exact interp 88,72 / prdp 89,27); los 2 improve de prdp son los Cube animados = fase, no exactitud, asi que la linea base de prdp NO se re-bendice. gate_all sube de 337 a 420 s: el retardo del SI es trabajo de invitado real, no un cuelgue. |
| 2026-09-08 | gate_all | 404 s | gate_prdp | 330 s | **Antialias de cobertura, las DOS mitades.** El AA de bordes del N64 no es del RDP ni del VI: es de los dos. (A) El RDP no guarda ALFA en el framebuffer, guarda **cobertura** de 3 bits. En RGBA5551 el bit alto va en el bit 0 de la palabra y los DOS bajos en el **noveno bit** de los chips RDRAM (plano oculto, ahora `Memory::rdramHidden`, un byte por palabra de 16 bits = donde el RCP los escribe); en RGBA8888 la cobertura ocupa `alpha = cvg << 5`; en I4/I8 la memoria devuelve 7. Con eso se pudo escribir la cadena entera de `blendPixel`: expansion del alfa del combinador, `CVG_TIMES_ALPHA`, `ALPHA_CVG_SELECT`, descarte con `cvg == 0` (el pixel no existe), `overflow = (cvg + memCvg) >= 8` que APAGA `blend_en` contra fondo lleno (por eso el AA no ensucia el interior de las superficies), `COLOR_ON_CVG` y los cuatro modos de `CVG_DEST` (CLAMP/WRAP/FULL/SAVE). (B) El borde suave lo dibuja el **VI** al barrer, no el RDP: `src/video/vifilter.{hpp,cpp}` nuevo con los tres filtros del hardware portados del oraculo de shaders de parallel-rdp (`vi_fetch.frag` / `vi_divot.frag` / `extract_vram.comp`) -- AA de 6 vecinos con los dos mejores por arriba y por abajo (`offset = second_lo + second_hi - 2*mid`, `coeff = 7 - cvg`, redondeo `(off*coeff + 4) >> 3` y envoltura de 8 bits), de-dither 3x3 para los pixeles de cobertura llena y divot (mediana de 3 salvo si los tres vecinos estan llenos). Puertas de `VI_CTRL` respetadas: tipo 2/3, `aa_mode < 2`, bit 4 divot, bit 16 de-dither; con `aa_mode >= 2` la cobertura se lee como 7 y eso apaga AA y divot a la vez, igual que el `extract_vram.comp`. **Donde SI y donde NO**: el filtro vive en el presentador (que es lo que se ve) y en el volcado determinista va apagado tras `KESTREL_VIFILTER=1`, porque las referencias de PeterLemon son capturas del FRAMEBUFFER y no del barrido del VI -- medido: con el filtro puesto en el volcado la suite BAJA (RSPGradient 100,00 -> 95,42, FillRectangle16BPP 98,64 -> 97,48, media 88,73 -> 88,69), y el caso que lo prueba es el de-dither sobre un degradado con tramado. Plano oculto = estado de invitado -> foto de estado **v7 -> v8** (sin el, el campo siguiente a cargar sale con los bordes mal). 15/15 + prdp 3/3, seis tests unitarios ALL PASS, `Base 0/3721 Timing 0/2 Cycle 0/6` en los siete modos. krom interp 371/371 `mean_exact` 88,73 **regress=0 improve=1** (`RDP/AlphaCoverage` 72,28 -> 74,76, que es justo el test que mide esto) y baseline re-bendecida; md5 de SM64 `195a464c...` -> **`d35bd8aa9b13d459ce9332c07a79a53a`** igual en los siete modos, divergencia querida (la etapa A cambia el bit 0 / el alfa del framebuffer) y comprobada a ojo con el titulo del juego. prdp 371/371 `mean_exact` 89,27 regress=0, sus 2 improve son los Cube animados = fase, baseline NO re-bendecida. Detalle en `docs/RDP-COVERAGE-AA.md`. |
| 2026-09-09 | gate_all | 410 s | gate_prdp | 331 s | **`Memory::rdpPace`: el RDP no costaba tiempo de INVITADO, costaba tiempo de PARED.** El freno entre dominios del RCP solo existia para el RSP; el worker del RDP tardaba lo que tardase el rasterizador del anfitrion y la CPU emulada seguia retirando instrucciones en su bucle de espera del `DP_DONE`, que como el reloj de video se deriva de `cpu.retired` se cobraban como TIEMPO DEL JUEGO. DK64 escena del rap, 3000 intercambios: lockstep+jit 6153 campos, `KESTREL_SYNCRDP=1` 6153 (exculpa al RSP), enhebrado+jit+SoftRDP **10830** (18,0 fps de invitado contra 30 en consola), enhebrado+jit+PRDP 6161, y con el freno puesto **6174**. El RDP tiene el MISMO reloj que el RSP (GCLK = reloj del RCP = 62,5 MHz), asi que el ratio `paceCpuNum/paceCpuDen` vale tal cual; la medida de trabajo sale del modelo de coste que ya existia (`SoftRdp::accountPixels`/`accountTmem`) publicada en un espejo MONOTONO `rcp.rdpGclk` (`dpc_clock` no vale: el invitado lo borra por DPC_STATUS bits 6..9). `rcpPace` pasa a combinar los dos frenos y devuelve el permiso mas corto. Ademas **puerta OCTOGONAL del stick** (`padOctagon`): el tope del stick es fisico y tiene ocho lados (~85 en eje, ~69 en diagonal), y kestrel recortaba por EJES a +-80 = puerta CUADRADA, con lo que la diagonal daba modulo 113, un valor que la consola no puede producir, y los juegos que sacan la velocidad de andar del modulo del stick corrian en diagonal un 33% de mas; zona muerta del mando del anfitrion de por-ejes a RADIAL con reescalado desde su borde. Y **`KESTREL_SPEEDMODE=hw` deja de clavar 59,94 Hz sin ventana**: queda en automatico (limitar solo si hay ventana), porque sin pantalla no hay nada que respetar y el resultado no cambia ni un bit; `KESTREL_THROTTLE=0/1` explicito sigue mandando. 15/15 + prdp 3/3, seis tests unitarios ALL PASS, `Base 0/3721 Timing 0/2 Cycle 0/6` en los siete modos, md5 de SM64 `d35bd8aa9b13d459ce9332c07a79a53a` igual en los siete y `b5521b24d8fc280fbf102df22d7d30cb` en prdp. krom interp 371/371 `mean_exact` 88,73 / `mean_close` 92,08 y prdp 371/371 89,27 / 92,57, ambos clavados en baseline, nodump=0. |
| 2026-09-09 | gate_all | 445 s | gate_prdp | 312 s | **Con parallel-RDP el RDP no le costaba NADA al invitado.** El reloj de coste (`rcp.rdpGclk`) y `DPC_CLOCK` solo los alimentaba `SoftRdp::accountPixels`, y `Memory::rdpRunJob` sale antes por la rama de `vrdp::active()`: con la GPU dibujando, el RDP salia GRATIS -- `DPC_CLOCK` clavado a cero (un registro que los juegos LEEN para medirse) y `Memory::rdpPace` sin morder nunca, o sea que la CPU del invitado jamas esperaba al RDP y el juego corria mas suelto de lo que corrio en consola. Arreglo: `Memory::rdpCostPass` pasa el MISMO tramo de FIFO por el decodificador de SoftRdp en modo `costOnly`, que mantiene el estado (scissor, modos, imagenes) y cobra los pixeles que entrarian al pipeline pero **no escribe un byte de RDRAM** -- los pixeles buenos siguen siendo los de la GPU. Coste O(altura) por primitiva, no O(area): cada tramo se cobra de una vez. Medido en Donkey Kong 64: `DPC_CLOCK` 0 -> 192.586 GCLK por campo = **18,5 %** de ocupacion del RDP (1.042.709 GCLK/campo a 62,5 MHz / 59,94 Hz). Coste en pared del paseo (SM64, 5e9 instrucciones, PRDP, min de 3): 42 s con `KESTREL_RDPCOST=1` vs 41 s con `=0`, o sea <=2,4 %, al ras del ruido del anfitrion. 15/15 + prdp 3/3, `Base 0/3721 Timing 0/2 Cycle 0/6` en los siete modos, md5 de SM64 `d35bd8aa9b13d459ce9332c07a79a53a` en los siete y `b5521b24d8fc280fbf102df22d7d30cb` en prdp, ambos sin cambio. krom interp 371/371 `mean_exact` 88,73 / `mean_close` 92,08 con regress=0 improve=0; krom prdp 371/371 89,27 / 92,56 con regress=0 e improve=2, los dos cubos giratorios `CubeFillTriangle{16,32}BPP320X240` 53,09 -> 55,27, que son tests de ANIMACION y caen en otro fotograma al cambiar el ritmo. nodump=0 en ambos. |
| 2026-09-10 | gate_all | 441 s | gate_prdp | 319 s | **El RSP no tiene tope de instrucciones por tarea.** `Rsp::start()` armaba `budget = 40'000'000` y al agotarse metia un BREAK forzado (HALT|BROKE + MI_SP): eso es la suposicion libultra *una tarea = un BREAK*. libdragon corre `rspq` como cola PERSISTENTE (`RSPQCmd_WaitNewInput` solo hace break cuando la cola se VACIA), asi que un juego que la mantiene llena encadena decenas de campos sin parar y se comia el tope; el BREAK nuestro destruia el estado de la cola -> congelacion de junkrunner64/SpellCraft, y detras los `SP->RDRAM dram=0x000000` con la cabecera de tarea ya basura. En HW no existe tal tope y quien devuelve el control al llamante ya era `maxInsns`. Ahora `Rsp::watchdog()` solo AVISA (limitado: 4 primeros y 1 de cada 64) y re-arma; tope duro opcional con `KESTREL_RSPBUDGET=<n>` para bisecar. junkrunner64 a 600 flips pasa en las cuatro combinaciones THREADS x parallel-RDP (antes `THREADS=0 PRDP=1` daba rc=124). De propina, `[dma!] ... sobre vectores` tambien era invariante de libultra (libdragon usa RDRAM baja: `dram=0x0001a0`/`0x0001a8`): pasa a opt-in `KESTREL_DMAWARN=1` y cortado a 16 lineas. 15/15 + prdp 3/3, systemtest 0/3721 en los siete modos, md5 sm64 sin cambio, krom regress=0 y nodump=0 en ambos (mean_exact interp 88,73 / prdp 89,27; improve=2 = los dos Cube animados, ruido de fotograma). |
| 2026-09-10 | gate_all | 430 s | gate_prdp | 323 s | **IO_BUSY de `PI_STATUS` no se apagaba solo.** `cartWrite` ponia el bit 1 y solo `cartRead` lo bajaba -- patron libultra (escribir y luego leer del cartucho). libdragon escribe y espera en `PI_STATUS` sin tocar el cartucho (`dma_wait()`: `lw v0,16(v1)` / `andi v0,v0,3` / `bnez`), asi que snapper64 giraba para siempre en `pc=0x8005b6c8` y no llegaba a configurar el VI (`origin=0, width=0` tras 1151 campos). En HW la escritura pendiente dura el ciclo del bus del PI y el bit cae sin que nadie lea nada. Nuevo `Memory::piIoDecay()` deja vencer el latch que YA existia (`CART_LATCH_TTL`, reloj de invitado) tambien al leer el registro; `cartRead` pasa a reusarlo. Una lectura inmediata tras la escritura sigue viendo IO_BUSY, igual que en HW. snapper64 arranca y renderiza (`fields=4440, flips=4025, origin=6651904, width=320` en <15 s). 15/15 + prdp 3/3, systemtest 0/3721 en los siete modos, md5 sm64 sin cambio, krom 371/371 regress=0 nodump=0 en ambos (improve=2 = los dos Cube animados, ruido de fotograma). |
| 2026-09-10 | gate_all | 387 s | gate_prdp | 318 s | **Las ROMs de libdragon iban lentas por dos cosas nuestras, ninguna el RDP.** (a) `emitInterpOp` (jit.cpp) absorbia LWL/LWR/SWL/SWR pero NO LDL/LDR/SDL/SDR, y el bloque se declinaba entero en la primera: GCC (libdragon) hace las copias desalineadas de 64 bits con `ldl/ldr`+`sdl/sdr` donde IDO (libultra) usa las de 32, y el bucle caliente de junkrunner64 (52% del perfil, `0x8001b184`) las lleva cada cuatro instrucciones. (b) la escritura de `SP_STATUS` llamaba a `rspAwaitIdle()` ante CUALQUIER CLEAR_HALT suelto, pero con el nucleo ya corriendo eso es un no-op en HW; `rspq_flush_internal()` (rspq.c:1165) lo escribe en cada vaciado de cola y dos veces seguidas, asi que la CPU emulada se clavaba hasta que el RSP drenaba la cola entera -- 37% de pared. Ahora la espera va atada a `wasHalted`, la misma condicion que ya usaba el lanzamiento. Descartado antes el sospechoso obvio: con `KESTREL_PACESLACK` gigante (freno del RDP fuera) el tiempo no se movia. junkrunner64 600 flips: 21 s -> **17 s**, 64,7 -> **82,6 Mips**, velocidad N64 89,3% -> **115,9%**, cpuWait 37% -> **5%**. SM64 de propina 151 -> 161 Mips (172% -> 186%). Lockstep sin tocar (133 s). 15/15 + prdp 3/3, systemtest 0/3721 en los siete modos, md5 sm64 sin cambio en las dos ramas, krom 371/371 regress=0 nodump=0. |
| 2026-09-10 | gate_all | 397 s | gate_prdp | 391 s | **La captura de telemetria metia la COBERTURA en el canal alfa del PNG.** El ultimo byte de un pixel de 32bpp (y el bit 0 de uno de 16bpp) no es alfa: es la cobertura que el VI usa para el antialias de bordes, y el DAC saca siempre imagen opaca. `cmdViCapture` lo copiaba tal cual a alfa, asi que todo lo que dibuja la CPU -- que deja cobertura 0 -- salia transparente y el visor lo pintaba blanco: el menu de snapper64 parecia una pantalla en blanco cuando en RDRAM estaba perfectamente dibujado. Solo se veia lo que habia pintado el RDP. Ahora la captura sale opaca (la cobertura, si hace falta, es un plano aparte). Afecta solo a la ruta SoftRDP de `capture_framebuffer`; `vrdp::scanout()` ya entregaba RGBA opaco. Cambio de telemetria puro, ningun efecto en emulacion: systemtest 0/3721 en los siete modos, md5 de sm64 sin cambio (interp `d35bd8aa`, prdp `b5521b24`), krom regress=0 y nodump=0 en ambos (mean_exact interp 88,73 / prdp 89,27; improve=2 = los dos Cube animados, ruido de fotograma). |
| 2026-09-10 | gate_all | 458 s | gate_prdp | 328 s | **Los registros DPS (`0x0420_0000`) no existian.** Es el puerto de test con el que la CPU lee y escribe el buffer de tramos interno del RDP: `DPS_TBIST`, `DPS_TEST_MODE`, `DPS_BUFTEST_ADDR` (7 bits) y `DPS_BUFTEST_DATA`. No lo implementa nadie -- ni ares, ni libdragon, ni parallel-rdp. La forma del RAM interno es lo que mide `snapper64`: la ventana da la vuelta cada 128 palabras = 32 entradas de tramo de 4 ranuras, y solo tres ranuras tienen registro fisico (la tercera guarda **solo 8 bits**, la cobertura; la cuarta no existe: lee 0, las escrituras se pierden). Implementado en `memory.{hpp,cpp}` con `dpsSpanRead/Write`, foto de estado v8 -> **v9**. `RDP Test-Mode - Span R/W` pasa de **0000/0032** a **0032/0032** (4096 asertos). systemtest 0/3721 en los siete modos, md5 sm64 sin cambio (interp `d35bd8aa`, prdp `b5521b24`), krom regress=0 nodump=0 (mean_exact interp 88,73 / prdp 89,27, improve=2 = los dos Cube animados). release.sh rc=0 en 67 s. |
| 2026-09-10 | gate_all | 439 s | -- | -- | Corrida con `KESTREL_CACHECOST` encendido a mano para medir CPI (no es un cambio de codigo). Anotada aqui solo para que el reloj de pared quede registrado. |
| 2026-09-10 | gate_all | 403 s | -- | -- | Corrida de certificacion del coste de accesos NO cacheados (`KESTREL_UNCACHEDCOST`). Suma de los pasos, sin el build. |
| 2026-09-10 | gate_all | 426 s | gate_prdp | 348 s | **Un plazo tiene que nacer y morir en el mismo reloj.** `Memory::siDma` arma el plazo del joybus en `cartNow()` (retiradas + pendientes del JIT + paradas de cache) y el bucle principal lo vencia contra `cpu.retired` a secas. Con el coste de cache apagado los dos relojes son el mismo y no se nota; con `KESTREL_CACHECOST=60`, `siDoneAt` nace desplazado por TODAS las paradas acumuladas desde el arranque en vez de por lo que dura la transaccion, asi que `SI_STATUS.DMA_BUSY` se queda pegado, no llega `MI_SI`, `osContStartReadData` no vuelve y el kernel de libultra nunca despierta al hilo del juego: **SM64 daba 300 campos de video con 0 intercambios de buffer, 0 syncs del RDP y 0 lecturas de mando**, con el 99,04 % del perfil en su hilo ocioso (`0x80246DD8 beq zero,zero,80246dd8`). Dependia de la magnitud (`=2` 87 intercambios, `=8` 1, `=30`/`=60` 0) y no era del dynarec (el interprete igual). Arreglo: vencerlo en `cartNow()`. De paso, dos deudas del mismo patron: `countTicksMax()` salia por el atajo sin mirar `fpuMode`, asi que con `KESTREL_FPUCOST=block` la guarda de borde de timer del JIT devolvia `ops` ignorando el suelo de 57 ciclos de `DIV.D`; y la guarda del plazo del SI del JIT comparaba `siDue` (reloj de invitado) contra `K` (ops), ahora contra `kTicks = countTicksMax(K)`, la misma cota que el borde de timer. Herramientas nuevas para bisecarlo, que se quedan: `KESTREL_MAXFIELDS=<n>` (tope de parada por CAMPOS de video -- `MAXFLIPS`/`MAXSYNCS` cuentan trabajo del RCP y no sirven justo cuando el juego arranca y no dibuja) y `KESTREL_STALLCLOCK` (`1` defecto / `vi` / `cart` / `0`, biseca el ACOPLE de las paradas al reloj de invitado sin apagar el cobro ni la telemetria). Resultado: SM64 con `CACHECOST=60` pasa de 0 a **95 intercambios / 96 syncs / 96 lecturas de mando** en 300 campos, igual que sin coste. Con las perillas apagadas -- que es como corren las puertas -- todo queda byte a byte: 15/15 + prdp 3/3, systemtest `Base 0/3721 Timing 0/2 Cycle 0/6` en los siete modos, md5 de sm64 `d35bd8aa9b13d459ce9332c07a79a53a` en los siete y `b5521b24d8fc280fbf102df22d7d30cb` en prdp, krom interp 371/371 `mean_exact` 88,73 / `mean_close` 92,08 con regress=0 improve=0, krom prdp 371/371 89,27 / 92,56 con regress=0 e improve=2 (los dos Cube animados, ruido de fotograma), nodump=0 en ambos. `release.sh` rc=0 en 70 s. |
| 2026-09-10 | gate_all | 410 s | -- | -- | **El hipo del audio estaba en el cebado del sumidero, no en el ritmo.** Medido primero, para no arreglar lo que no fallaba: DK64 es PAL (detectado bien, 50,00 campos/s), el AI abre a 22 049 Hz y la produccion real, restando tandas para quitar el silencio del arranque, es de **22 043 Hz = 0,03 % de desviacion** (250->500 campos: 110 216 fotogramas en 5,0 s; 500->1000: 220 432 en 10,0 s). Ni deriva de tono ni de velocidad. Lo de 'acelerado' es correr SIN limitador: 500 campos PAL = 10,0 s de invitado, medido 10,48 s con `KESTREL_THROTTLE=1` y **5,70 s = 1,76x tiempo real** con `=0`; el automatico limita solo si hay ventana, asi que una sesion con ventana ya iba bien. El fallo de verdad: `feederLoop` (src/audio/audio.cpp) esperaba un colchon de `kBufSamples * 2` (dos bufers) y en cuanto lo tenia encolaba los **cuatro** bufers del dispositivo de golpe -- los dos ultimos salian a medias porque `ringPull` rellena de ceros lo que no hay, y la comprobacion de 'se acabo el colchon' estaba DESPUES de servir el hueco. Firma inconfundible: **9 616 muestras de silencio por arranque, la MISMA cifra corriera 250, 500 o 1000 campos**, o sea un hipo fijo al empezar a sonar y no hambre continua. Arreglo: `kPrimeSamples = kBufSamples * kNumBufs` (el colchon minimo es lo que se encola de golpe) y comprobar que hay bufer ENTERO ANTES de sacarlo, descebando y saliendo del bucle si no lo hay (lo ya encolado sigue sonando mientras se rehace el colchon). DK64 250/500/1000 campos: silencio 5,69 %/2,61 %/1,17 % con 17 recargas cortas -> **0,00 %/0,00 %/0,00 % con 0 cortas**; SM64 600 campos a 32 006 Hz igual, 0,00 %. Cambio del anfitrion puro, no toca un bit de estado de invitado: 15/15, systemtest `Base 0/3721 Timing 0/2 Cycle 0/6` en los siete modos, md5 de sm64 `d35bd8aa9b13d459ce9332c07a79a53a` sin cambio, krom interp 371/371 `mean_exact` 88,73 / `mean_close` 92,08 con regress=0 improve=0 new=0, nodump=0. `release.sh` rc=0 en 55 s. gate_prdp no se repite: la corrida de 348 s de mas arriba cubre el mismo binario en todo lo que ve el invitado. |
| 2026-09-10 | gate_all | 460 s | gate_prdp | 368 s | **El RSP corre mientras la CPU esta parada en la cache.** Segunda entrega de 'un plazo nace y muere en el mismo reloj', esta vez sin cuelgue. El acoplamiento CPU:RSP medi­a la CPU en instrucciones RETIRADAS en dos sitios: el interleave de Lockstep (`rspPhase += rspStepNum` una vez por instruccion, src/core/system.cpp) y el regulador de Threaded (`Memory::rcpPace`/`rspPace`/`rdpPace` con `paceCpu0 = cpuRetired`, mas la llamada del prologo del dynarec en jit.cpp). El ratio de `Clocks` es RSP por instruccion-EQUIVALENTE de CPU, y una instruccion que falla en la D$ cuesta ~60 ciclos de latencia de RDRAM durante los cuales el RSP sigue corriendo a 62,5 MHz: contando solo retiradas le damos menos pasos de los que le tocan, y el sesgo aprieta justo en las escenas con mas fallos. Los dos lados miden ya en `cpu.guestOps()` (retiradas + paradas convertidas a equivalentes), el mismo reloj del campo de video, el plazo del SI y el latch del PI; el interleave usa el DELTA por instruccion (`rspPhase += rspStepNum * dGuestOps`) y reengancha la referencia al salir de un bloque del dynarec, que solo se toma con el RSP parado. Con `KESTREL_CACHECOST` apagado `dGuestOps` vale 1 y queda byte a byte igual. Medido en DK64 PAL a 300 campos: sin coste Lockstep y Threaded coinciden EXACTO (74 intercambios / 56 syncs / 402M retiradas los dos); con `CACHECOST=60` Lockstep pasa de 74/31/316M a **76/31/320M**, determinista 3 de 3 antes y despues. Destapa ademas un fallo ANTERIOR al cambio y mas gordo: **el modo Threaded no es determinista**, con perilla o sin ella. DK64 a 300 campos con `CACHECOST=0` da 72/70/73/73 intercambios y 56/53/55/56 syncs en cuatro corridas, `origin` alternando, mientras Lockstep clava 74/56 tres de tres; SM64 da 71/95/95 contra 95 fijo. Tampoco es el acople (`STALLCLOCK=0` sigue bailando). Causa: los workers levantan `MI_SP` y `MI_DP` en tiempo de PARED, asi que el invitado ve la interrupcion en una instruccion distinta cada corrida. Anotado en docs/GAPS.md 3b; las puertas no lo cazan porque validan Lockstep==Threaded con SM64 a 60 campos, que aun no ha divergido. Verificacion: siete modos `Base 0/3721 Timing 0/2 Cycle 0/6`, md5 de sm64 `d35bd8aa9b13d459ce9332c07a79a53a` (interp) y `b5521b24d8fc280fbf102df22d7d30cb` (prdp) sin cambio, krom interp 371/371 88,73/92,08 regress=0 improve=0 new=0, krom prdp 371/371 89,27/92,56 regress=0 improve=2 (los dos Cube animados de siempre), nodump=0. |
| 2026-09-10 | gate_all | 460 s | gate_prdp | 370 s | **Threaded ya es determinista (docs/GAPS.md 3b, RESUELTO).** Dos piezas. (1) *Coste primero*: en `Memory::rdpRunJob` el paseo de coste (`rdpCostPass`, SoftRdp en `costOnly`) va DELANTE de `vrdp::runFifo`, con punteros de reanudacion propios (`rdpCostResume`/`rdpCostLastEnd`); antes iba detras y dejaba `rcp.rdpGclk` clavado durante todo el trabajo, asi que `rdpPace` frenaba contra cero mientras la GPU dibujaba y el plazo de fin nacia VENCIDO. (2) *Barreras de invitado*: la CPU no puede pasar del instante de invitado en que termina la tarea en vuelo -- `dpBarrierOps` (publicada en cuanto el paseo de coste conoce el coste) y `spBarrierAt()` = `spKickOps + ops(cyclesRun - spKickCycles)` (el contador del RSP ya es monotono y se publica cada pocos miles de instrucciones), bits 2 y 3 de `rcpPend`, metidas en `rcpDueIn` para que un bloque del dynarec no se las trague, con salvavidas de 20 ms. Diagnostico que lo caso: traza por campo `KESTREL_FIELDTRACE=1` (diff de dos corridas para el primer campo que difiere) y los contadores `spLate`/`dpLate` (plazos nacidos vencidos). Descartados por medida: `PACESLACK=0 PACEGRAIN=1` (solo movia la divergencia del campo 195 al 206) y `KESTREL_DPFAKE=1` (lecturas DPC del RSP deterministas). Resultado: `spLate`=`dpLate`=**0** en las dos ROMs; DK64 PAL 300 campos clava la traza entera 13 corridas de 13 (74 intercambios / 56 syncs / `origin=0283c0`) y SM64 4 de 4 (95 / 96 / `3b5280`), y Lockstep da la MISMA traza (diff = 0 lineas). Coste: DK64 900 campos, 11,67 s con las dos barreras contra 10,58 s sin la del RDP = ~10 %, y sin ella el determinismo se cae (415 -> 417 intercambios); la del RSP sale gratis (11,64 s). Queda un resto: `Rsp::cyclesRun` varia ~0,8 % entre corridas de SM64 porque el microcodigo sondea `DPC_CURRENT`/`DPC_STATUS`, que hoy publica el worker segun el avance de la GPU en tiempo de pared; el estado visible del invitado ya no cambia por eso. Verificacion: siete modos `Base 0/3721 Timing 0/2 Cycle 0/6`, md5 de sm64 `d35bd8aa9b13d459ce9332c07a79a53a` (interp) y `b5521b24d8fc280fbf102df22d7d30cb` (prdp) sin cambio, krom interp 371/371 88,73/92,08 regress=0 improve=0 new=0, krom prdp 371/371 89,27/92,56 regress=0 improve=2 (los dos Cube animados de siempre), nodump=0. |
| 2026-09-11 | gate_all | 481 s | gate_prdp | 376 s | **Horario del RDP en tiempo de envio + reloj exacto del RSP + apagon del anfitrion.** (1) `rdpSubmit` corre el modelo de coste bajo `rdpMx` y publica el tramo entero (`dpScheduleSpan`): instante de arranque, de cierre, las fronteras de `DPC_CURRENT` y el plazo de `MI_DP` existen ANTES de pintar; el trabajador solo pinta. El anillo pasa a ser por TRAMO (`kDpRingN=256`), y `DPC_CURRENT`/`DPC_STATUS` se interpolan del horario (`dpcCurrentFor`/`dpcStatusFor`) en vez de leerse del pintor. (2) El RSP publica su reloj exacto (`exactCycles()`), asi que su instante de invitado ya no se redondea al trozo de 8192. (3) **`Rsp::hostStop`**: apagar el emulador no es un HALT del invitado. Un microcodigo puede estar girando legitimamente en la espera de FIFO de F3DEX2 (`t8 < DPC_CURRENT <= t8+s3`), que solo desbloquea la CPU instalando el siguiente buffer; si la CPU ya paro en el tope de campos, esa espera no termina nunca y `stopRcpThreads()` se quedaba en el join. Diagnosticado con el volcado `[dpwait]` (mostraba `sub=218 compAt=218 pend4=0`, `cur=0x4cc2c8 == t8+s3` exacto). DK64 pasa de 3/6 corridas colgadas a 6/6 saliendo en 4-5 s. Determinismo de DK64 a 300 campos: todas las columnas menos `rsp=` identicas hasta f=206 (antes f=192); las de `rsp=` son multiplos exactos de 8192 = artefacto de telemetria. 8/8 modos PASS/MATCH, md5 unico `d35bd8aa9b13d459ce9332c07a79a53a`; krom interp 371/371 mean_exact=88.73 regress=0, krom prdp 371/371 mean_exact=89.27 regress=0 improve=2 (Cube FillTriangle 16/32BPP 53.09 -> 55.27). |
| 2026-09-11 | gate_all | 436 s | gate_prdp | 341 s | **Aparcamiento del RSP en la espera del FIFO: DK64 en Threaded ya es determinista bit a bit, columna `rsp=` incluida.** La fuga que quedaba NO era orden de envio (`ooo=0` en todas las corridas, la barrera de invitado del SP ya ata la CPU por detras del RSP), sino *lecturas de horario rancias*: el microcodigo sondea `DPC_CURRENT` desde un instante POSTERIOR al de la CPU, y cuando la CPU instala el siguiente buffer lo sella con `kick = cartNow()`, o sea ANTES de instantes ya contestados. 42-47 tramos por cada 300 campos, todos del hilo de la CPU. Arreglo: (1) `Rsp::idleSkip` reconoce el bucle de espera del FIFO de F3DEX2 (IMEM 0x2a0, cinco instrucciones sin efecto lateral) por su firma leida del flujo -- mismo PC, mismo FNV-1a de `r[1..31]`, mismo valor devuelto, misma distancia en ciclos que la lectura anterior, cuerpo de 2 a 64 ciclos y motor drenado en `now` (`Memory::dpDrainedAt`). Nada hardcodeado: cualquier sondeo sin efecto lateral sobre un `DPC_CURRENT` constante entra, y cualquier cosa que toque un registro sale. (2) `Memory::rspParkWait` aparca al RSP: con el motor drenado el chip no puede levantar interrupcion, ni escribir `DPC_END`, ni tocar memoria, asi que la barrera del SP no protege nada y se levanta (`spBarrierEff` devuelve `rspPark + kParkLead`, `kParkLead = 1<<24` ops), y `rspPace` deja de frenar. (3) Lo despierta `dpScheduleSpan` con el `kick` del tramo que acaba de archivar -- valor de INVITADO, luego determinista; el destino del salto nunca es `cartNow()` (tiempo de anfitrion puro: metia en el reloj del RSP lo lejos que la CPU hubiera llegado en esa corrida concreta, y descuadraba `rsp=` en +-16 ciclos ya por el campo 6). (4) Se le cobran las iteraciones enteras que caben: `k = rcpOpsToCycles(tgt - now) / len`, `cyclesRun += k * len`. El salvavidas de anfitrion se reinicia con el progreso, al estilo de `rspPace` (lo intolerable es que la CPU se ATASQUE, no que sea lenta): DK64 tiene dos tramos (f=137 y f=140) donde la CPU se arrastra a ~1,2 M ops/s y un corte plano de 200 ms saltaba ahi; con el reinicio, `park=48/0`, cero indultos. SOLO en Threaded: en Lockstep los dos chips comparten hilo, aparcar al RSP aparca al unico que hay y el que tendria que despertarlo -- la CPU -- no puede correr; DK64 se quedaba muerto desde f=67. `KESTREL_RSPIDLE=0` lo apaga y el md5 del framebuffer no se mueve (es atajo de anfitrion, no de semantica). Ademas la cita de lectura del FIFO (`dpReadSync`, `KESTREL_DPRDV`) pasa a nacer APAGADA: resolvia lo mismo por la via cara (el RSP esperaba a la CPU en CADA sondeo) y su ventana `kRdvLead` era la unica causa de que 10-13 fechas del SP nacieran tarde; medido en DK64 300 campos, aparcamiento solo 9 s con `spArm 173/0 tarde` y rastro identico 4/4 incluyendo `rsp=`, contra aparcamiento+cita 11 s con `spArm 173/11 tarde` y rastro identico solo quitando `rsp=`. Se deja a mano porque cubre un caso que el aparcamiento no toca: sondear `DPC_CURRENT` con un tramo ABIERTO y por delante de la CPU (~235 sondeos de 13,9 M en DK64, ninguno diverge). Resultados: `build/` (SoftRDP) Threaded 3/3 rastros byte-identicos md5 `b43236f9c027f4a0cbaf19df040e0ffc`, `build-prdp/` Threaded 3/3 md5 `d3bee5263f26e4dca3519e277220e090`, `build-prdp/` Lockstep identico al binario de antes del cambio; `[det]` identico entre corridas (`rspCycles=60771479` prdp / `60771383` soft, `stale=0`, `ooo=0`, `spArm=173/0 tarde`, `dpArm=56/0 tarde`, `park=48/0`, `idle=48/6621806`, `dpcRd=5658`). Antes las columnas de invitado solo cuadraban hasta f=206 y `rsp=` derivaba sobre 104-115 M. Coste: 13,9 M sondeos emulados pasan a 5,7 k, y la corrida de 300 campos va de 6 s (no determinista) a 9 s SoftRDP / 8 s Parallel-RDP -- el RSP deja de quemar ciclos de anfitrion en el giro pero el aparcamiento serializa los dos hilos a grano de tarea; recuperar ese solape es el siguiente punto de rendimiento y es independiente de la correccion. Ambas puertas verdes y MAS RAPIDAS que la linea base (436 s vs 481 s, 341 s vs 376 s): 8/8 modos PASS, sm64 `d35bd8aa9b13d459ce9332c07a79a53a` (soft) / `b5521b24d8fc280fbf102df22d7d30cb` (prdp), krom interp 371/371 mean_exact 88,73 regress=0, krom prdp 371/371 mean_exact 89,27 regress=0 **improve=2** (`CubeFillTriangle16BPP320X240` y `32BPP`, 53,09 -> 55,27). |
| 2026-09-11 | gate_all | 468 s | gate_prdp | 334 s | **Dos carreras que quedaban en el aparcamiento del RSP, y el rastro de campos hecho atomico.** La fila de arriba decia 3/3 con rastro identico; ampliando a 6 corridas salia 1 de cada 3-4 divergiendo, siempre con `spArm=.../1 tarde`. Eran dos cosas distintas. (1) *Carrera de publicacion del despertar*: `dpScheduleSpan` mira `rspPark` para decidir si deja la fecha de despertar, asi que un tramo archivado entre que `idleSkip` ve el motor drenado y que `rspParkWait` publica el aparcamiento no dejaba ninguna -- el aviso se perdia y el RSP se quedaba hasta el tope o hasta el tramo SIGUIENTE segun como se cruzaran los dos hilos, que es tiempo de anfitrion puro. Arreglo: `idleSkip` captura `dpSubSeq` en el mismo sitio donde comprueba `dpDrainedAt` y se lo pasa a `rspParkWait` como `seq0`; la espera comprueba a mano si el contador se ha movido -- antes de aparcar y en cada vuelta -- y si se ha movido usa `dpJobStartG[seq0]`, que con el motor drenado ES el lanzamiento de ese tramo. La carga con acquire de `dpSubSeq` es la que hace visible lo que escribio `dpScheduleSpan`. Contador nuevo `rspParkMiss`. (2) *La barrera del SP se quedaba abierta durante toda la latencia de despertar*: `spBarrierEff` devolvia `rspPark + kParkLead` mientras `rspPark` estuviera puesto, pero la fecha de despertar la pone el PROPIO hilo de CPU al archivar el tramo, asi que desde ese instante el RSP va a reanudar justo donde esta la CPU y la barrera tiene que volver a mandar. Sin eso la CPU seguia libre hasta que el worker se enteraba, la tarea acababa en un instante ya rebasado y la fecha de fin del SP nacia tarde. Arreglo: `spBarrierEff` y el adelanto de `rspPace` solo levantan el freno si `rspParkWake` sigue a cero. (3) Ademas el `[ft]` se escribia con `fprintf` y salia partido cuando otro hilo imprimia a la vez: una linea rota se lee como divergencia que no existe. Ahora se arma en un `char[192]` y se suelta con un solo `fwrite`. Con las tres: `build-prdp/` Threaded **6/6** byte-identico md5 `d3bee5263f26e4dca3519e277220e090`, `build/` (SoftRDP) **4/4** md5 `b43236f9c027f4a0cbaf19df040e0ffc`, Lockstep 2/2 md5 `ce4ec2bc2d6298cb69d270607db199ee` e IDENTICO a la corrida con `KESTREL_RSPIDLE=0` (el salto no cambia nada donde no actua), sm64 Threaded con `KESTREL_RSPIDLE=0` sigue en `d35bd8aa9b13d459ce9332c07a79a53a`. `spArm=173/0 tarde` y `park=48/0` en las seis. Entra tambien la cobertura de savestate del horario nuevo: NO se serializa (es horario derivado, no estado de la maquina, y el estado se toma siempre con el RCP en reposo) sino que se REINICIA al cargar con `Memory::rcpSchedReset` desde `afterLoad` -- anillo de 256 tramos, `dpSchedEnd`, `dpSubSeq`/`dpCompSeq`, `dpMaxQuery`, `dpLastKick`, `rcpPend`, los plazos, `rspRdvAt`/`dpRdv`, `rspPark`/`rspParkWake` y la firma del bucle de espera del RSP; los anclajes de la barrera del SP se vuelven a atar al reloj que acaba de entrar. Sin esto un `dpSchedEnd` de la partida anterior dejaba el motor eternamente ocupado contra el reloj restaurado. Con el anillo a cero `dpcCurrentFor` cae en `rcp.dpc_current`, que si va en el fichero. `System::quiesceRcp` despierta antes al RSP aparcado (`rspParkNudge`, mismo destino que le habria dado el tope): si no, la CPU se para en seco, nadie archiva el tramo que lo despertaria y la espera solo acabaria por el salvavidas de 200 ms. Y fuera el diagnostico `[ds+]`, que imprimia ciclos del RSP y `cartNow()` -- tiempo de anfitrion en un rastro que se compara entre corridas. Puertas verdes: 8/8 modos PASS, `rsp_test`/`save_test`/`cheat_test`/`archive_test`/`wildmem_test`/`rewind_test` ALL PASS, sm64 `d35bd8aa9b13d459ce9332c07a79a53a` (soft) / `b5521b24d8fc280fbf102df22d7d30cb` (prdp), krom interp 371/371 mean_exact 88,73 regress=0, krom prdp 371/371 mean_exact 89,27 regress=0 improve=2. |
| 2026-09-11 | gate_all | 484 s | gate_prdp | 343 s | **Medidor real de ocupacion del bus de RDRAM (el HUD ya no dice `RAM0%`).** `System::rdramSpeedPct` era `{0.0}` y no lo escribia nadie. Ahora se cuentan BYTES por maestro donde el trafico ocurre de verdad: CPU (`CPU::ramCpuBytes`, contador liso porque solo lo toca el hilo de CPU -- relleno de linea de D$ 16 B, volcado de linea sucia 16 B, relleno de I$ 32 B, y accesos NO cacheados que caen dentro de la RDRAM, tanto del interprete como de los ayudantes del dynarec), RSP (`spDma`: `length * count`, el salto entre filas no se transfiere), RDP (derivado de la MISMA lista de transacciones que ya usa el modelo de coste calibrado en `SoftRdp::accountPixels` -- por chunk, `ciBpp` si hay IM_RD mas 2 B si hay Z_CMP, mas la escritura de color y la de z cuando el pixel no muere; y `accountTmem` en bytes directos: cero constantes nuevas que calibrar), VI (relee el framebuffer entero cada campo, una linea por linea de salida, `vi_width * lines * bpp` con `lines` sacado de `VI_V_START` en medias lineas), PI, AI y SI. El porcentaje se divide entre el pico de la consola (562,5 MB/s) **por segundo de tiempo de INVITADO** (`retiradas / Clocks::insnTarget()`), no de pared: es una propiedad del juego y sale igual corra el emulador al 20% o al 300%. Linea nueva `[rdram]` al terminar y en el `[hb]`, con el reparto por maestro en MB/s. Medido: SM64 titulo **7,8 % del bus** (43,8 MB/s: cpu 8,3 rdp 20,3 vi 9,1 rsp 5,7 pi 0,3), DK64 400 campos **11,4 %** (64,3 MB/s: cpu 7,5 rdp 30,2 vi 21,8 rsp 4,2). El VI pesa lo que decia el modelo (320x237x2 a 60 Hz son ~9 MB/s con la pantalla quieta), que es la comprobacion de que el numero no esta inventado. Es un SUELO declarado: no entran el refresco de RDRAM, el noveno bit (paridad/cobertura oculta) ni las lecturas del latch del cartucho. Tambien: `rspParkMiss` sale ya en `[det]` (`park=parks/salvavidas/carreras-cazadas`). Nada de esto es entrada de control -- atomicos `relaxed` y contadores lisos --, y se comprueba: DK64 Threaded en `build-prdp` 3/3 `d3bee5263f26e4dca3519e277220e090`, el MISMO md5 de antes del cambio. Puertas verdes: ocho modos `Base 0/3721 Timing 0/2 Cycle 0/6`, sm64 `d35bd8aa9b13d459ce9332c07a79a53a` / `b5521b24d8fc280fbf102df22d7d30cb`, krom interp 371/371 88,73 regress=0, krom prdp 371/371 89,27 regress=0 improve=2. |
| 2026-09-11 | gate_all | 466 s | gate_prdp | 344 s | **El plazo de la barrera del SP se media con el reloj equivocado: DK64 300 campos 7,4 s -> 2,83 s.** El aparcamiento del RSP de las filas de arriba no serializaba los dos hilos (eso fue una atribucion sin medir): lo que se caia era el DYNAREC. `Memory::rcpDueIn` calculaba el plazo del bit 8 (`rcpPend`, fin de barrera del SP) con `spBarrierAt()` = `spKickOps + rcpCyclesToOps(rsp.cyclesRun - spKickCycles)`, y con el RSP APARCADO su `cyclesRun` esta congelado por definicion, asi que ese valor se queda clavado detras del invitado y `rcpDueIn` devolvia **0** durante todo el aparcamiento; en `jitTryBlock`, `if(siDue <= kTicks) return 0;` declinaba entonces CADA bloque, la concesion encadenada (`jitGuard`) se venia abajo y la CPU bajaba a interpretar de una en una justo en la ventana en la que es el unico hilo que puede desatascar la escena (el RSP espera el buffer que ella instala). Quien decide donde se para la CPU no es `spBarrierAt()` sino `spBarrierWait`, que mira `spBarrierEff()` -- con el RSP aparcado y sin fecha de despertar publicada vale `rspPark + kParkLead`, que es exactamente hasta donde la CPU tiene permiso. Arreglo de una linea: `u64 b = spBarrierEff();`. Cadena de diagnostico, y las tres piezas se quedan: linea **`[block]`** al terminar (reparto del tiempo de PARED entre espera de CPU -- freno / barrera SP / barrera DP --, ocupacion y aparcamiento del RSP, ocupacion del RDP, mas el tiempo de CPU REAL de los tres hilos via `GetThreadTimes`), que decia que con el aparcamiento no habia NADA ocupado y aun asi sobraba un 63 % de pared; **`rspParkNs`**, porque el sueno del aparcamiento cae DENTRO de `rsp.step()` y sin descontarlo `rspBusyNs` contaba como trabajo un hilo dormido (DK64 marcaba "rsp 92 % ocupado" con 2 % de CPU real), ahora se descuenta en `rspWorkerLoop`; **`cpuCpuNs`** (mismo muestreo para el hilo de CPU) daba `cpu 99,8 %`, o sea el hilo de CPU QUEMABA ciclos de anfitrion en vez de dormir; y `KESTREL_JIT_STATS` remataba con **43 M** entradas al driver del JIT (`cover=3,9 % avgK=0,08`) contra **menos de 1 M** sin aparcamiento, retirando las dos corridas las MISMAS 402 M instrucciones. Ademas `KESTREL_PARKLOG` (opt-in, clase `KESTREL_DPSYNCLOG`) imprime `[pk]` por aparcamiento con via de salida, salto, `cartNow()` y vueltas: los 48 salian `via=wake spins=3..10`, o sea 60-300 ms de pared para avanzar 520 k-1,2 M ops de invitado. Medido (DK64 PAL, 300 campos, `build-prdp`, Threaded): aparcamiento ON 7,33-7,71 s -> **2,83 s**; OFF (`KESTREL_RSPIDLE=0`) 4,0-4,5 s -> 4,01 s; `[block]` con ON pasa de `cpuWait 2,7 % / rsp ocupado 2,2 % aparcado 76,5 % / rdp 3,9 % / CPU real cpu 99,8 %` a `cpuWait 7,3 % (freno 0,0 barSP 0,3 barDP 2,2) / rsp ocupado 5,6 % aparcado 47,0 % / rdp 10,0 % / CPU real cpu 100,3 %`. El aparcamiento ya no cuesta nada: sale MAS RAPIDO que apagarlo y mas rapido que los 6 s de la linea base no determinista de antes de todo esto. Determinismo: 3/3 trazas byte-identicas md5 `ccf3fd5bf216c429db341ddc5164df31`, `[det]` clavado (`rspCycles=60771443`, `spArm=173/0 tarde`, `dpArm=56/0 tarde`, `ooo=0`, `stale=0`, `idle=48/6621800`, `park=48/0/0`); contra un binario de ANTES del arreglo reconstruido a proposito (`d3bee5263f26e4dca3519e277220e090`, `rspCycles=60771479`, `idle=48/6621806`) difieren 44 lineas de 300, desde f=257, y SOLO en la columna `rsp=` -- quitandola las dos dan el mismo md5 `417ae0aa5c60abf6dae491c9cd4af994`, y `ret`/`ops`/`gclk`/`sp`/`dp`/`flips`/`syncs`/`org`/`mi` son identicas byte a byte; los 36 ciclos de desplazamiento vienen de que el largo del bloque del JIT cambia la cuantizacion del `kick` que la CPU estampa en los tramos que archiva, que entra en `k = rcpOpsToCycles(tgt - now) / len` dentro de `idleSkip`. Puertas verdes y por debajo de la linea base (466 s vs 481, 344 s vs 376): ocho modos `Base 0/3721 Timing 0/2 Cycle 0/6`, sm64 `d35bd8aa9b13d459ce9332c07a79a53a` (interp) / `b5521b24d8fc280fbf102df22d7d30cb` (prdp), krom interp 371/371 `mean_exact` 88,73 / `mean_close` 92,08 regress=0 improve=0 new=0, krom prdp 371/371 89,27 / 92,56 regress=0 improve=2 (los dos Cube animados), nodump=0 en ambos. |
| 2026-09-11 | gate_all | 488 s | gate_prdp | 347 s | **El freno del prologo del dynarec frenaba tambien al RSP aparcado (DK64 300 campos 2,98 s -> 1,71 s, -43 %).** `[tramp] guard/MI/timer/rsp/otro` cantaba **22 M** rebotes por el RSP en una corrida, con el `[block]` de la misma corrida diciendo que el RSP estaba **aparcado el 47,1 % de la pared**. El camino rapido del prologo (`src/cpu/jit.cpp` ~1181-1245) mira un byte del RSP antes de saltar al cuerpo y encadenar; ese byte era `Rsp::running`, que sigue puesto durante todo `Memory::rspParkWait`. Asi que la cadena se rompia en cada eslabon -- una llamada Win64 al trampolin por bloque -- justo en la ventana en la que la CPU es el UNICO hilo que puede desatascar la escena, porque es ella quien archiva el tramo cuyo `kick` despierta al RSP. Y ahi el freno no regula nada: un RSP aparcado no ejecuta microcodigo, no escribe MMIO y no puede levantar interrupcion, y a la CPU la siguen acotando la barrera de invitado del SP (`spBarrierEff()` -> `rspPark + kParkLead`) y `rcpPace`, las dos en reloj de invitado. El freno hace falta cuando el RSP SI corre, y solo entonces. Cambio: bandera `Rsp::brake` = `running` MENOS el aparcamiento, en la misma linea fria de 64 bytes que `running` (que esta `alignas(64)` aislada a proposito: la invalidacion entre nucleos costo una vez el 18 % del emulador entero); se pone/quita en los cinco sitios donde `running` cambia de verdad y ademas `rspParkWait` la suelta antes de publicar el aparcamiento y la repone antes de retirarlo. El prologo y la clasificacion del trampolin miran `brake`; la comprobacion de Lockstep en `jitReenterProceed` sigue mirando `running`, que es lo que quiere saber ahi. Resultado: pared 2,98 -> **1,71 s**, `[tramp] rsp=` 22 M -> 0, rastro de campos **byte identico** `ccf3fd5bf216c429db341ddc5164df31` 3/3 (el mismo md5 que antes del cambio), Lockstep `ce4ec2bc2d6298cb69d270607db199ee` 2/2, `[det]` sin mover ninguna columna salvo `await=`. `[block]` despues: `pared 1.71 s | cpuWait 14.4% (freno 0.0% barSP 0.6% barDP 4.3%) | rsp ocupado 9.6% aparcado 10.8% | rdp ocupado 16.8% | CPU real: cpu 99.5% rsp 6.3% rdp 14.6%`. Cierra el apunte "recuperar el solape CPU/RSP perdido con el aparcamiento": el aparcamiento ya no cuesta nada, es la mas rapida de las tres configuraciones. Puertas: 8/8 modos `Base 0/3721 Timing 0/2 Cycle 0/6`, `rsp_test`/`save_test`/`cheat_test`/`archive_test`/`wildmem_test`/`rewind_test` ALL PASS, sm64 `d35bd8aa9b13d459ce9332c07a79a53a` (interp, seis modos) y `b5521b24d8fc280fbf102df22d7d30cb` (prdp), krom interp 371/371 88,73 / 92,08 regress=0 improve=0 new=0, krom prdp 371/371 89,27 / 92,56 regress=0 improve=2 (los dos Cube animados de siempre), nodump=0. |
| 2026-09-11 | gate_all | 473 s | gate_prdp | 347 s | **El rastro por campo llevaba cuatro columnas medidas en tiempo de anfitrion.** `KESTREL_FIELDTRACE=1` existe para comparar por md5 dos corridas del mismo binario y encontrar el primer campo que difiere; cuatro de sus once columnas no servian para eso. (1) `sp=`/`dp=` eran ARMADOS (`spArms`/`dpArms`), y en Threaded a `spEndArm`/`dpEndArmAt` las llama el hilo del RSP/RDP cuando termina el trabajo en tiempo de PARED: el plazo que arman si va en reloj de invitado y lo publica el hilo de CPU en el instante que le toca -- el invitado nunca ve nada distinto -- pero el contador sube en un campo o en el siguiente segun como se crucen los hilos. Contadores nuevos `spRets`/`dpRets`, que suben SIEMPRE donde la interrupcion se hace visible al invitado: `Memory::rcpFlushPending` (camino normal, hilo de CPU) y los cuatro sitios de publicacion directa. `[det]` sigue enseñando `spArm=`/`dpArm=`, que es donde ese dato tiene sentido. (2) `rsp=`/`gclk=` son `Rsp::cyclesRun` y `rcp.rdpGclk`, avance real de los workers, y no tienen arreglo posible: pasan a `KESTREL_FIELDTRACE=2`. El nivel 1, el que se difunde y se compara, solo lleva columnas que el invitado puede ver. Resultado: DK64 PAL 300 campos `4fc7dc59e262d5a8a0cd2c89712b8ae3` **3/3 en Parallel-RDP y 2/2 en SoftRDP, el mismo md5 en los dos motores**; SM64 NTSC `a022f09184a1fa61917565cc7909630f` **6/6**. Falsa alarma que queda escrita: SM64 parecia no determinista (2 trazas en 6 corridas) y no lo era -- la primera corrida no encontraba `Super Mario 64 (USA).eep` (lo crea al salir) y las demas si, o sea estado inicial distinto; sin partida guardada el juego tarda 72 campos mas en llegar a la primera tarea grafica (71 intercambios contra 95 en 300 campos). Borrar el guardado antes de cada corrida forma parte del experimento. Y `ret`/`ops` NO valen para ver progreso: son `viFields * instrucciones-por-campo`, el reloj, e salen identicos con el juego parado. ABIERTO (`docs/GAPS.md`): DK64 Lockstep `8c18b4c8cfbd806a1cbb8f4d5168cc2d` 2/2 contra Threaded `4fc7dc59...` difieren 79 lineas de 300 desde f=191, primera columna `syncs=` (`rcp.dpSyncs`, que este cambio no toca) -- preexistente. Puertas: 8/8 modos `Base 0/3721 Timing 0/2 Cycle 0/6`, seis `*_test` ALL PASS, sm64 `d35bd8aa9b13d459ce9332c07a79a53a` / `b5521b24d8fc280fbf102df22d7d30cb`, krom interp 371/371 88,73 / 92,08 regress=0 improve=0 new=0, krom prdp 371/371 89,27 / 92,56 regress=0 improve=2, nodump=0. |
| 2026-09-11 | gate_all | 458 s | gate_prdp | 350 s | **El hilo ocioso del invitado se cobra de golpe (`CPU::jitIdleSkip`, `KESTREL_CPUIDLE=0` lo apaga).** `beq $0,$0,-1` con `nop` en la ranura de retardo no escribe registro, no toca memoria y solo produce Count: en DK64 son 251,5 M de 402,3 M instrucciones de invitado en 300 campos (62,5 %, todas en `0x80000a08`, medido con `KESTREL_PCSAMPLE=0x80000000`, que ahora saca tambien histograma por instruccion de la pagina cuando el valor es una direccion). Se cobran de golpe (retired + Count + Random, el mismo commit del trampolin) y el limite del salto es EXACTAMENTE el permiso que `jitReenterProceed` da a una cadena enlazada -- borde de Compare, `siDueIn`, `rcpDueIn`, `jitOpsBudget`, `rcpPace` en Threaded y `kGuardMaxOps` = 4096 -- o sea que no cambia CUANDO se mira cada evento, solo deja de emular lo que hay entre dos miradas; se redondea a iteraciones enteras para no partir el par salto+ranura. Se NIEGA con `Status[2:0] != 1` (sin interrupcion posible el invitado esta colgado de verdad y saltarle el reloj lo esconderia) y en Threaded sin plazos de RCP (`KESTREL_RCPDEADLINE=0`, unico modo donde un worker publica `MI_SP`/`MI_DP` en tiempo de pared sin cota de invitado). Telemetria `[ocioso] saltos=/ops=` dentro de `KESTREL_JIT_STATS`. Medido en SM64 `bench` 200 intercambios (597 campos = 9,96 s de video, minimo de 3, threaded-jit): **5,13 s con el salto contra 5,98 s sin el, -14,2 % de pared**, 194,2 % de tiempo real contra 166,6 %. Puertas verdes y por debajo de la linea base (458 s vs 473-488, 350 s vs 347): seis `*_test` ALL PASS, ocho modos `Base 0/3721 Timing 0/2 Cycle 0/6`, sm64 `d35bd8aa9b13d459ce9332c07a79a53a` en los siete modos y `b5521b24d8fc280fbf102df22d7d30cb` en prdp/prdp-jit, krom interp 371/371 `mean_exact` 88,73 / `mean_close` 92,08 regress=0, krom prdp 371/371 89,27 / 92,56 regress=0 improve=2 (los dos `CubeFillTriangle` animados, 53,09 -> 55,27), nodump=0 en ambas. Ademas, trabajo de lanzador sin efecto en el emulador: la biblioteca del lanzador web pasa a 3D de verdad (cajas con volumen en WebGL, colocaciones anillo/coverflow/pared al estilo USB Loader GX, matriz por tramo de malla y casillas recicladas por modulo; `node tools/launcher/carousel_test.js` TODO BIEN). |
| 2026-09-11 | gate_all | 473 s | gate_prdp | 381 s | **El orden de las optimizaciones vuelve a salir de una medida** (`docs/GAPS.md`, seccion Rendimiento). Perfil de anfitrion con `KESTREL_HOSTPROF=1` sobre SM64/`build-prdp`/threaded-jit/PRDP/400 intercambios: 906 muestras del hilo de CPU, 769 en la imagen, simbolizadas contra `llvm-nm --numeric-sort` con `ImageBase 0x140000000`. `Memory::spBarrierWait` 43,43 % · `Memory::dpBarrierWait` 34,46 % · `kestrel_jitProceedTramp` 6,63 % · `Memory::rspPace` 5,72 % · `CPU::jitIdleSkip` 5,07 % · `Memory::rdpPace` 1,30 % · `Memory::rcpPace` 0,65 % · `CPU::jitTryBlock` 0,65 %. `dcFill`/`dcFlush`, que ENCABEZABAN la lista vieja, no aparecen: el orden anterior era de otra epoca del emulador. Cruzado con `[block]` (`cpuWait 20,2 %` -- freno 0,0 %, barSP 1,2 %, barDP 31,5 % --, `rsp ocupado 65,7 %`, `rdp ocupado 16,8 %`) ese 43 % NO es tiempo bloqueado sino la espera ACTIVA de 2048 vueltas: el palo largo de SM64 es el RSP. **Cambio de codigo**: pista `_mm_pause` en las tres esperas activas (`Memory::spBarrierWait` y las dos fases de `dpReadSync`), a una de cada 16 vueltas porque PAUSE cuesta ~9 ciclos en Nehalem y ~140 de Skylake en adelante; apagable con `KESTREL_SPINPAUSE=0` y largo del giro con `KESTREL_BARSPIN=<n>` (0 = defecto 2048), las dos en el lanzador (avanzadas, via `tools/launcher/options.py` -> `src/ui/optdefs.cpp`). **Medida honesta: NEUTRA en este anfitrion.** Un primer A/B de 3 corridas daba -2,3 %, pero intercalado a dos rondas de 5 (`bench --mode threaded-jit`) el minimo sale OFF 4,93 s / 5,04 s contra ON 4,98 s / 4,98 s -- ruido. Se queda porque es la semantica correcta de una espera activa y el i7-870 es justo el caso donde menos da; no se apunta una ganancia que no existe. **Telemetria**: `[det] idle=` pasa a `idle=saltos/vueltas(sigN/drnN/roomN)` (`Rsp::idleNoSig`/`idleNoDrain`/`idleNoRoom`, no atomicos porque solo los toca el hilo del RSP, puestos a cero en `Memory::rcpSchedReset`). Con el queda CERRADO el `idle=0/0` de SM64: los 1 041 389 sondeos de `DPC_CURRENT` se van enteros por `sig`; el PC si se repite (1 038 015) y la longitud cae en [2,64] en 665 177, pero la huella de `r[1..31]` no coincide NI UNA VEZ y el valor solo 1 356, porque el RDP esta masticando (`rdp ocupado 54,5 %`, `open=317 k`) y el valor se mueve en cada vuelta. No hay bucle de espera con valor fijo que aparcar: no es un fallo. El sondeo tampoco es el gasto (~5 M de instrucciones de RSP sobre 291 M de ciclos, 1,7 %). Certificacion: seis `*_test` ALL PASS; ocho modos `systemtest Base 0/3721 Timing 0/2 Cycle 0/6`; sm64 `d35bd8aa9b13d459ce9332c07a79a53a` en los siete modos de SoftRDP y `b5521b24d8fc280fbf102df22d7d30cb` en prdp/prdp-jit; krom interp 371/371 `mean_exact` 88,73 / `mean_close` 92,08 regress=0 improve=0; krom prdp 371/371 89,27 / 92,56 regress=0 improve=2 (los dos `CubeFillTriangle` animados, 53,09 -> 55,27, fase de animacion); nodump=0 en ambas. NOTA: `build-prdp` se reconstruyo con el cambio de `PaceDiv` ya escrito en el arbol, asi que esta corrida de `gate_prdp` cubre ademas el reciproco exacto; `build` (gate_all) es de antes y cubre solo la pista PAUSE. |
| 2026-09-11 | gate_all | 480 s | gate_prdp | 373 s | **`PaceDiv`: el freno del RCP deja de dividir (exacto, no aproximado).** `Memory::rspPace`/`rdpPace` salian a 7,0 % juntos en el perfil de anfitrion del dia, y las dos rutas hacen una division de 64 bits por el ratio de reloj CPU/RSP (65536/43691, NO 3/4). Sustituida por reciproco EXACTO, no aproximado: `struct PaceDiv` guarda `m = ceil(2^79/d)` y el limite `maxX = floor(2^79/e)` con `e = m*d - 2^79` (0 <= e <= d); dentro de ese limite `floor(x*m >> 79) == floor(x/d)` para TODO `x`, y por encima -- o si `m` no cabe en u64 -- cae a division de verdad. Demostrado ademas por fuerza bruta: 32 000 070 casos (todos los `x` pequenos mas barridos alrededor de `maxX` y de los multiplos de `d`) sin una sola discrepancia. `setPaceRatio()` queda como UNICO sitio que toca el ratio, asi que `m`/`maxX` no pueden quedar desincronizados del divisor. **Medida honesta: NEUTRA en este anfitrion.** A/B intercalado de dos rondas de 5 corridas (`bench --mode threaded-jit`, minimo de cada ronda): con reciproco 4,95 s / 4,96 s, con division 4,93 s / 4,98 s -- ruido. El 7 % NO era el divisor: son las cargas `acquire` de `rspBusy` y `rsp.cyclesRun`, dos lineas de cache que el worker del RSP reescribe sin parar (fallo de cache compartida, inherente al acoplamiento CPU-RCP). `paceGrant(ahead, allow)` ya las lee una sola vez por vuelta de trampolin y `kPaceGrain` (1024, `KESTREL_PACEGRAIN`) ya evita la cola de permisos que encogen. Se queda el reciproco porque es exacto y mas barato en el peor caso, pero no se le apunta ganancia. Certificacion completa con el cambio en el arbol: seis `*_test` ALL PASS; ocho modos `systemtest Base 0/3721 Timing 0/2 Cycle 0/6`; sm64 `d35bd8aa9b13d459ce9332c07a79a53a` en los siete modos de SoftRDP y `b5521b24d8fc280fbf102df22d7d30cb` en prdp/prdp-jit; krom interp 371/371 `mean_exact` 88,73 / `mean_close` 92,08 regress=0 improve=0; krom prdp 371/371 89,27 / 92,56 regress=0 improve=2 (los dos `CubeFillTriangle` animados, 53,09 -> 55,27, fase de animacion); nodump=0 en ambas. |
| 2026-09-16 | gate_all | 441 s | gate_prdp | 354 s | Comentario de `rspjit.hpp` (sin cambio de codigo emitido). |
| 2026-09-16 | gate_all | 518 s | gate_prdp | 354 s | **Giro del worker del RDP ocioso (`KESTREL_RDPSPIN`, defecto 32768).** Antes de dormir en `rdpCv` gira sobre `dpPending`; ahorra despertar el hilo por el kernel en cada DPC_END. Bench SM64 200 intercambios, min de 5: prdp-jit 3,98 -> 3,45 s (-13 %), threaded-jit 4,93 -> 4,63 s (-6 %). Meseta desde 32768. gate_all 518 s INCLUYE la recompilacion de `build/` dentro del gate (fuentes tocadas despues del build), no es cuelgue. md5 sm64 `d35bd8aa`/prdp `b5521b24` iguales, krom interp 88,73/92,08 regress=0, prdp 89,27/92,56 regress=0 improve=2. |
| 2026-09-16 | gate_all | 445 s | gate_prdp | 339 s | **Notificar al RSP solo si duerme + giro previo en las esperas del RDP.** `rspSubmitKick` y el fin de tarea ya no hacen `notify_all` incondicional (`rspIdleWaiting`/`rspWaiters`, patron Dekker con `rspMx`): 3,55 -> 3,51 s. `KESTREL_RSPSPIN` (giro del worker del RSP entre tareas) medido NEUTRO en 0/4096/32768/1M, defecto 0. `dpBarrierWait` y `rdpAwaitGuest` giran `KESTREL_DPSPIN` (2048) vueltas sobre `rcpPend&4`/`dpSchedEnd`/`dpCompSeq` antes de dormir en `rdpCv`: A/B 3/3 rondas 3,49/3,49/3,51 -> 3,46/3,46/3,43 (~-1,5 %); barrido 0/2048/16384/65536 plano dentro del ruido. DESCARTADO por medida: memorizar la division de `rcpCyclesToOps` en el giro de `spBarrierWait` (12 % del perfil del hilo de CPU): neutro, porque el giro quema el mismo tiempo de pared -- dividir menos solo da mas vueltas. Todos los modos md5 iguales, krom regress=0. |
| 2026-09-16 | gate_all | 444 s | gate_prdp | 339 s | **`KESTREL_DPSPIN` defecto 2048 -> 262144.** Barrido min de 5, dos rondas: prdp-jit 2048 3,44-3,46 / 262144 3,42 / 1 M 3,41-3,42 s; threaded-jit (SoftRDP) 2048 4,55 / 262144 4,51 / 1 M 4,49-4,50 s. Meseta desde 262144 (cubre el fence de SYNC_FULL de la GPU, ~1,4 ms por cuadro). `KESTREL_BARSPIN` barrido 2048/65536/1 M con DPSPIN=262144: plano dentro del ruido, se queda 2048. md5 iguales, krom interp regress=0, prdp regress=0 improve=2. |
| 2026-09-16 | gate_all | 446 s | gate_prdp | 331 s | **MFC0/MTC0 dentro de los bloques del dynarec del RSP** (`Rsp::jitCop0`, corte al volver si SET_HALT o DMA a IMEM). SM64 prdp-jit 400 intercambios: interpretadas 8,26 M -> 0,70 M (99,8 % JIT), entradas 8,52 M -> 0,71 M. Bench min de 5, 3 rondas: prdp-jit 3,41-3,42 s y threaded-jit 4,51 s en A y B, neutro; CPU real del hilo RSP igual. Se queda: reloj exacto por instruccion y menos interprete. md5 todos iguales, krom interp regress=0, prdp regress=0 improve=2. |
| 2026-09-16 | gate_all | 437 s | gate_prdp | 301 s | **Parallel-RDP procesa los comandos en el worker del RDP, sin su hilo `CommandRing`** (`PARALLEL_RDP_SINGLE_THREADED_COMMAND=1` por defecto desde `vrdp::init`; =0 vuelve al anillo). El aviso de ocio que daba el anillo (`Op::MetaIdle`) lo da `vrdp::idle()`: el worker al dormirse tras el giro, o el cierre de campo en lockstep/`KESTREL_RDPINLINE`. Sin el, `HelloWorldRDP16BPP` (lista sin SYNC_FULL) salia negra (100 -> 1). SM64 prdp-jit min de 5, 4 rondas: 3,41-3,45 -> 3,38-3,39 s (-1,6 %). gate_all 15/15, gate_prdp regress=0 improve=2, md5 iguales. |
